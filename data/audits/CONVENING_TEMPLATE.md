# CONVENING ARTIFACT — REQUIRED SECTIONS (rig section Q enforces the strings)
1. covers: <sha>  — the exact tree audited.
2. Findings + fix-now list. Behavior changes = Marcos's call (auditor-cannot-authorize, 8/13).
3. DAY-ONE WALKTHROUGH — trace the mechanism's intended first specimen end-to-end through the
   NEXT session, unchanged components included, until it produces the promised row or the trace
   names what stops it. (Instituted 8/13 after floor/flip; first outing caught the Kev-read
   inversion the same night.)
4. DOCTRINE-INVERSION SWEEP — if the ship changes or follows a DOCTRINE change (who rules, what
   gates, what's exempt): enumerate every place in the WHOLE system that encodes the OLD doctrine,
   unchanged files included (the 8/5 skip-if-kev-levels rule survived 4 audits because it was
   correct code for a repealed premise). If no doctrine is touched, write literally:
   "doctrine-inversion sweep: n/a". The string "doctrine-inversion" MUST appear either way.
5. ROLL CALL — every office on ROSTER.txt by name (touched w/ finding, or clean w/ reason).
