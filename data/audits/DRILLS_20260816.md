# PROVING DRILLS — Sunday 2026-08-16 (from EYES_AUDIT_20260815.md BUILT-UNPROVEN list)

Clock: `date` = Sun Aug 16 11:44 EDT 2026 (start) → 12:20 EDT (write). Bot deployed and asleep (weekend);
nothing that needs a live scan loop can be proven today — those get the exact Monday command + expected rows.
Every PROVEN(today) claim below is from an execution run THIS session (command + output cited); nothing from code reading alone.

## ⚠️ HEADLINE FINDING (affects D6 and every "Monday first-fire" drill)
Production evidence says the running bot is the **8/14 14:01 build**, i.e. PRE-08e8639:
- `/api/decisions_archive?date=2026-08-14&status=boot_config` → 5 boot rows (03:55, 09:54, 12:54, 12:57, **14:01:57** = last);
  none carries `e3_exits` / `grinder_convert` / `flattop_break_attack` (those knobs were added to the boot row in 08e8639 @ 20:31 8/14).
- No `boot_config` row on 8/15 or 8/16 (`decisions_archive` 8/15 = 4 rows, all `kev_sweep` — so weekend decision posting works; a restart would have shown).
- Auto-deploy was DISABLED ~17:40 8/14 (ledger 1970-1973); ship.sh is the only door and no ship is ledgered after that.
- Therefore **08e8639 (grinder/break-attack/E3), 50601ed (dead-eyes fix incl. `premkt_gate_armed`), and all 8/16 builds (acf89f8/070ed5d/3af6207/7ead311) are NOT in production.**
  Memory's "Monday's deployed machine (all shipped 8/14)" is contradicted by the boot rows. [PRODUCTION EVIDENCE, not code reading]
- Action for whoever deploys before Monday 07:00: `ship.sh` with the flat-book check in-turn (`/api/open_trades` = `[]` right now, 12:13 EDT — weekend, safe window). Without it, D6 cannot fire and D3's Monday drill still works (pause_entries path is old), but E3/grinder/break-attack first-fires will not exist.

## Per-drill status

### D1 STOP-COHERENCE forced-width — **PROVEN (today)**
Command: `python3 rig/test_stop_coherence_drill.py` → exit 0 (10/10 PASS).
Method: real module loaded via `rig/loader.py` (real `_log_decision`, real `_slot_refund`, real `_decision_queue`); the exact STOP-COHERENCE block text-extracted from `_trade_worker` (marcos_trading_bot.py:11857-11872) and exec'd in the module dict; synthetic trade entry $10.00 / stop $9.97 (0.3%) on `vwap_reclaim` with a pre-spent RTH ticket.
Rows produced (in-proc queue): `stop_coherence_refused {price 10.0, stop 9.97, width_pct 0.3, machine vwap_reclaim}` → `slot_refunded {machine vwap_reclaim}`; worker returned before sizing; `_curl_rth_n` ticket refunded; `reentry["held"]` released. Controls: 6% stop passes to sizing; exactly 0.5% passes (floor is strict `<`).
Still owed: first LIVE specimen (census says ~3/era — rare is plausible). Monday watch: `curl "$U/api/decisions?status=stop_coherence_refused&limit=20"`.

### D2 EOD WINNER SWEEP — **PROVEN by production footprint (8/14)**
The sweep writes no decision row; its trace is `POST /api/bars` per market-wide mover (count=960, sessions PRE+RTH+ATH; two passes 16:02 and 20:05 ET, `_winner_sweep_loop`).
Evidence: `GET /api/bars` archive lists **102 plain-name files for 2026-08-14** (153 on 8/13, 153 on 8/12) — market-wide, far beyond the 9 traded + 4 Kev names; `AKAN` 8/14 file = 960 bars ending **2026-08-14T23:59Z (19:59 ET)** = the after-hours 20:05 pass signature (only `winner_sweep`/`archive_daily_bars` request 960+ATH).
Caveat: 960/ATH is shared with the traded-tickers archiver; the 100+ name breadth is the sweep's alone. Railway log line (`🏁 winner_sweep 2026-08-14: archived N/M`) not readable from this seat.
Monday check (one command, after 20:10 ET): `curl -s "$U/api/bars" | python3 -c "import json,sys;a=json.load(sys.stdin)['archived'].get('2026-08-17',[]);print(len([n for n in a if '~' not in n and '__' not in n]))"` → expect ≥60.

### D3 KILL-UNDER-FIRE — **MONDAY-SCRIPTED** (script written, NOT run — refuses outside weekday 07:00-16:00 ET; refusal path exercised today: `--dry` → exit 3 "outside a live scan window")
File: `data/killtests/kill_under_fire_drill.py`. Compiles; dry path executed.
Command (Marcos-chosen minute, board hot): `DASHBOARD_SECRET=… python3 data/killtests/kill_under_fire_drill.py --watch 300 --expires 600`
Built-in: flat-book check (`/api/open_trades` must be `[]` else STOP), refuses if a freeze is already set, server-side `expires_in` so the freeze cannot outlive the operator, always CLEARs, then 3-min resume verify.
Expected row sequence: `entries_paused {ticker, price, machine}` → `slot_refunded {machine}` for each candidate that fires into the freeze (bot :11537-11544); dashboard log `[pause-entries] -> {'paused': True…}` then `-> {'paused': False…}`; after CLEAR, zero new `entries_paused` and normal `triggered_*`/`chart_gate_*` rows resume. Exit 0 = PROVEN; exit 4 = INCONCLUSIVE (no candidate fired in the window — re-run when hot); exit 1 = FAILED (freeze not accepted / not cleared / honored after clear).

### D4 RUNWAY-WALL live proof — **PROVEN (today, wall-insert branch, from 8/14 production rows post-fix)**; demotion branch MONDAY
File: `data/killtests/runway_wall_live_check.py <date> [HH:MM since]` — reconstructs the wall's own high (last 720 10s bars = `_curl_feed(n=720)`, ~2h window, from `~ALP10S` archive) at each `runway_pass`/`runway_reject` row time and classifies WALL_TARGET / DEMOTED / NO_EFFECT / SUSPECT.
Run today: `python3 data/killtests/runway_wall_live_check.py 2026-08-14 12:58` (fix ef55647 committed 12:36, live by ~12:58 per auto_map resurrection): **WALL_TARGET ×3** — 12:59:12 BANL runway_pass tgt=12.10 = window high 12.1000; 15:09:05 HAO runway_reject tgt=3.84 = 3.8400; 15:21:41 HAO runway_reject tgt=4.17 = 4.1700 (map targets 3.7/3.745 nowhere near → the target IS the wall). Pre-fix rows (09:35-12:46) show 17+ SUSPECT rows (target below window high) = the dead-wall era, consistent with the audit's founding exhibit.
Open: 1 post-fix SUSPECT — 14:44:08 DFSC tgt 2.96 vs window high 2.97 (0.34%; one-bar alignment between my archive read and the bot's live feed is a plausible cause — watch, not a verdict). DEMOTED (spent-rung skip) = 0 observed yet.
Monday one-command: `python3 data/killtests/runway_wall_live_check.py 2026-08-17` → expect ≥1 WALL_TARGET or DEMOTED and 0 SUSPECT (exit 0).

### D5 KEV UNCONDITIONAL READS at 07:0x — **PROVEN (8/14 store, 2/4 timestamped at 07:00) + MONDAY-SCRIPTED**
File: `data/killtests/kev_reads_0715_check.py [date]`. Run today on 8/14: sheet AKAN/DFSC/WETO/XHG all `src=vision` with `kev_shadow=yes` (the flip happened, Kev preserved); **AKAN read 07:00:11, XHG 07:00:28**; DFSC/WETO `_ts` = later re-reads (14:45 after `read_exhausted_observed`/`reread_latency`, 14:03) — first-read time not recoverable from `_ts`. Exit 0.
Monday: `python3 data/killtests/kev_reads_0715_check.py` at 07:15 ET → every sheet name `READ@07:0x (<=07:15)`; NOT YET before 07:30 = re-run; after = pull reader logs. Reader start = `NEWCOMER_START_HHMM=07:00` (ledger 1421, env-shipped) [prod env UNVERIFIED from this seat].

### D6 PREMKT heartbeat — **BLOCKED-BY-DEPLOY (code path read; will NOT fire on the running build)**
Path: `_eye_heartbeat("premkt_gate_armed", …)` at marcos_trading_bot.py:11400, inside the PRE branch (`_in_premkt = ENTRY_OPEN_ET <= now < "09:30"`), fires once per ET day on the **first scan with a non-empty PRE `breakouts` list**; fields cap=`PRE_MAX_TRADES`, slots_used, candidates, flat_hhmm=09:25. `_eye_beat_day` is in-memory (a restart re-stamps once; use first-row-of-day).
`ENTRY_OPEN_ET=07:00` verified from the 8/14 boot_config row (`entry_open_et: '07:00'`). Expected first stamp Monday: the first PRE scan ≥07:00 ET that has ≥1 candidate — realistically 07:00-07:10 on a normal gap day; absence with PRE candidates on the board = PRE loop not reaching the gate.
BUT: the heartbeat lives in 50601ed (23:03 8/14) which is not in production (headline finding). Zero `premkt_gate_armed` rows 8/14 as expected. Monday check: `curl -s "$U/api/decisions?status=premkt_gate_armed&limit=3"` — only meaningful after ship.sh.

### D7 GRADER LIVE EXECUTION — **PROVEN (today, regression of Sunday's edits)**
Command: `python3 data/killtests/nightly_shadow_grade.py 2026-08-14` → exit 0; line: `grinder N=0 $+0.00 | flat_top(in-win) N=0 (oow N=21 $-192.98) | v2 N=250 $-2943.88 | bandpass(in-win) N=0 (oow N=0) | prevwap(PRE,flat 09:25) N=0 | E3 PORTFOLIO $+0.00 | skipped 0` — identical dollars to the 8/14 proof line (flat_top oow $-192.98, v2 $-2943.88); new bandpass/prevwap columns render at N=0 (rows don't exist yet). No-lookahead: `sim_e3` skips bars `<= t0` (line 63).
Side effect handled: the run APPENDS to `data/history/OOS_WALL.md` (duplicate 8/14 line, "wall day 2") — reverted with `git checkout data/history/OOS_WALL.md`. **Finding:** the grader has no same-day dedupe; any manual re-run on a scheduled day double-counts "wall day N" — small guard owed (Statistician/Quartermaster).

## Summary
| Drill | Status |
|---|---|
| D1 stop-coherence forced-width | PROVEN(today) — rig exec on real namespace, refuse+refund rows, exit 0 |
| D2 EOD winner sweep | PROVEN(8/14 footprint: 102 names, AH bars to 19:59 ET) — Monday count check written |
| D3 kill-under-fire | MONDAY-SCRIPTED — `kill_under_fire_drill.py`, refusal path exercised |
| D4 runway wall | PROVEN(today, insert branch: 3 post-fix WALL_TARGET rows 8/14) — demotion branch Monday, 1 SUSPECT watch |
| D5 Kev unconditional reads | PROVEN(8/14: 4/4 flipped, 2/4 stamped 07:00) — 07:15 Monday script written |
| D6 premkt heartbeat | BLOCKED-BY-DEPLOY — code path fine, not in the running build |
| D7 grader regression | PROVEN(today) — exit 0, identical numbers, wall restored |

Officers touched: Blast Radius Auditor (D1 exec-on-real-namespace), Statistician (OOS_WALL restore + dedupe finding), Pit Crew Chief (deploy-state finding, flat-book gate in D3), Cartographer (D4 wall), Kev Librarian (D5), Opening Bell (D6), Quartermaster (D2 archive). Clean: Trade Manager, Execution Surgeon.
