# SHIP CONVENING — 2026-08-16 (Sunday, 11:31 ET) BAND-PASS VWAP RECLAIM RTH lane (shadow ON, BANDPASS_CONVERT env-OFF) + PRE-VWAP Kev-8AM shadow lane + grader status additions + rig AF
covers: 7ead311d36c6 "build: band-pass RTH lane (shadow, convert env-off) + pre-vwap Kev 8AM shadow" (3 files: marcos_trading_bot.py +161/-6, data/killtests/nightly_shadow_grade.py +18/-5, rig/test_shipset_20260804.py +102) on 28282e0 (prior bookkeeping) / 3af6207 (prior audited code). Prior convening archived to SESSION_20260816_curlmemo.md.
Chair: Blast Radius Auditor. Separate-context convening: every claim below is from a `git show`/file read or an execution run THIS session. Clock: `date` run this turn = Sun Aug 16 11:31:41 EDT 2026. No push, no deploy from this convening.

## SHIP DESCRIPTION + AUTHORIZATION
- New envs (:5896-5899): `BANDPASS_SHADOW` default "1", `BANDPASS_CONVERT` default "0", `BANDPASS_DAILY_CAP` default 3, `PREVWAP_SHADOW` default "1". Constants BP_HOLD_MIN=12 / BP_HOLD_MAX=30 / BP_BELOW_MIN=2 / BP_CHOP_CROSSES=3 / BP_CHOP_SECS=1200 / BP_COOL_SECS=900 (:5900-5905). Two module-level state maps `_bp_st`, `_pv_st` + `_bp_conv_day` cap ledger (:5906-5908).
- `bandpass_step(sym, new_bars, vwap, st_map, hm_lo, hm_hi)` (:5910-5964): shared detector, one fire max per call, returns dict or None.
- Caller inside the reclaim block of `wait_for_flat_top_entry` (:7543-7608): RTH lane rows `bandpass_shadow_fire` (in_window + convert_on stamped); conversion ONLY under `BANDPASS_CONVERT and _bp_in` (:7563), lane "bandpass", exit_mode E3, zone_stop = hold low, cap row `bandpass_capped`, `triggered_bandpass` row. PRE lane rows `prevwap_shadow_fire` (spread_pct stamp via one Webull quote, catalyst=None) — no conversion code at all.
- Grader `nightly_shadow_grade.py`: two new statuses in the archive query, `bandpass`/`bandpass_oow`/`prevwap` lanes, `sim_e3(..., flatten_hm)` (PRE = 565 = 09:25), wall line gains three columns.
- Boot banner + `boot_config` row stamp the four new switches (:11171-11172, :11205-11206). Rig section AF (a-n).
Authorization class: shadow rows default-on = observe-only (always safe). The RTH conversion path is a behavior change WITH MONEY and ships DEFAULT OFF — turning `BANDPASS_CONVERT=1` is Marcos's call (the commit header says "Marcos's evening call"); this room does not authorize it, it only certifies the path is gated, capped, and non-exempt.

## FINDINGS (each verified by direct read/execution this session)
1. **BANDPASS_CONVERT default "0"; with it off the lane is shadow-only.** `os.environ.get("BANDPASS_CONVERT", "0") == "1"` (:5897). The ONLY `breakouts.append(... "bandpass" ...)` in the file (:7576, grep = one hit) sits under `if BANDPASS_CONVERT and _bp_in:` (:7563), itself under `if _bpf:` under `if BANDPASS_SHADOW:`. Rig AF-a (env defaults, executed against the real segment), AF-h (guard precedes append, string-order), AF-k (executed guard shape, zero appends). Zero appends reachable with CONVERT=0.
2. **prevwap has NO conversion path anywhere.** grep `prevwap|_pv_st|PREVWAP` over marcos_trading_bot.py → only the env line (:5899), the state map (:5907), the caller block (:7587-7608), banner/boot row (:11172, :11206). The caller block contains no `breakouts.append`, no `execute_trade`, no `triggered`; the detector segment itself contains neither (rig AF-i, AF-j). PRE fires cannot become trades by any switch.
3. **Detector mechanics vs spec (read :5910-5964 + rig AF-b..g executed on synthetic tapes):** (a) two-bars-below arm — `armed = pa is not None and below_n >= BP_BELOW_MIN(2)` at the up-cross, below_n counts completed closes below (AF-c: 1 below → no fire); (b) 12-30 close hold — fire only when `BP_HOLD_MIN <= streak <= BP_HOLD_MAX`, streak counts closes above since the cross bar; `streak > 30` sets done (AF-e: 31 no fire, 3 no fire); (c) new-high fire — `h > hold_hi` where hold_hi = running high of the hold episode (AF-b: fires at streak 15 on the first bar whose high exceeds 10.10, px = that close 10.12); (d) chop exclusion — crosses list (both directions) trimmed to 20 min, `n_cross < 3` required (AF-d); (e) cooldown — `k - cool_k < 900` suppresses (AF-g), per-name because state is per sym; (f) stop = hold low (`hold_lo` = min low of the episode; AF-b 9.97) with `stop < c` required. One attempt per hold episode (`done` set before the checks — a cooled/chopped attempt consumes the episode; conservative, no re-fire on the same hold). Day-keyed state resets on a new ET date. Note: `bandpass_step` uses `datetime.now(EASTERN)` for the day key only; the window test uses the BAR's own k (:5926-5928) — correct for the rehydrate deep first pass (REHYDRATE_BARS) that feeds older bars.
4. **Hot path: rides the existing fed bars, no new fetches.** Both calls pass `_nb` — the same list built once per name from `_curl_feed` at :7441-7449 for kev_reclaim_step/hidden/v2/grinder — and `_vr_sv`. `_bp_st`/`_pv_st` are pure dict work. The PRE spread stamp (:7592-7598) is a per-FIRE `_get_webull_quote(t)` — fires are rare (one per hold episode per name, 15-min cooldown, 07:00-09:25 only) so worst realistic cost is a handful of quote calls per morning; `_get_webull_quote` runs the SDK call on the `_quote_executor` pool with `future.result(timeout=QUOTE_TIMEOUT_SECS=8)` (:556, :3594-3630) and returns {} on timeout/error, and the caller wraps it in its own try → sets `_pv_sp=None`. It cannot hang the loop beyond the existing 8 s cap that every quote call already carries; the whole PRE block is inside `try/except Exception: pass` (:7588, :7607). Not zero-cost — an 8 s stall on a fire is possible if Webull wedges — same exposure the monitor's quote path already accepts; noted, not blocking (shadow).
5. **[UNVERIFIED] flag RESOLVED — `_vr_sv` IS premarket-anchored 07:00-09:25.** `_vr_sv = _tickv if (_tickv and _tick_vwap_ok(...)) else vwap` (:7439). (i) Tick line: `_recorder_tick_vwap` (:6751) reads `~ALPVWAP` first (`_TICKVWAP_SUFFIXES` :6727) — alpaca_capture.py:40 documents ~ALPVWAP as "premarket-anchored even for late adds (Fable F3)" (anchored to the 4:00 open); the code header at :6722 says the same. (ii) Bar fallback: `ENTRY_VWAP_PREMARKET = True` (:6703, 7/15 validated "Kev trades the Webull VWAP, PM included") → the `elif` branch (:7350-7370) fetches sessions=["PRE","RTH"] and sets `cache[t]["vwap"] = _fullvw` (pre+RTH). Under RTH-only fallback (`ENTRY_VWAP_PREMARKET` False) full_bars is RTH-only and PRE vwap would be 0 → the whole reclaim block skips (`if _vr_sv and _vr_sv > 0` :7440), so prevwap could never fire on an RTH-only line — it either sees the PRE-anchored line or nothing. Caveat: `_tick_vwap_ok` (:6767) allows tick vs bar divergence ≤5% only; both are PRE-anchored so no anchor conflict. B17 (:7354-7361) holds a stale PM line ≤10 min else 0 → skip. Verdict: prevwap rows carry Kev's line (session-anchored incl. premarket). No docket item.
6. **Not in any exempt set; normal gates on convert.** `MIN_STOP_EXEMPT` default "zone_flip,hidden_entry,flat_top" (:7127), `BACKSIDE_EXEMPT = {"dip_rip"}` (:6847), `VRIDE_EXEMPT` "hidden_entry" (:492); grep `"bandpass"` = 3 hits, all in the caller/comment (:5890, :7546, :7576). Rig AF-l also checks `_STALE_EXEMPT`. A converted bandpass fire enters `breakouts` like grinder/flat_top conversions and walks the standard gate stack downstream (min-stop, back-side, momentum, liquidity, size). Stop delivered as `zone_stop` = hold low — same key grinder uses.
7. **Rig executed MYSELF**: `python3 rig/test_shipset_20260804.py` → **ALL GREEN, exit 0**, A..AF (AF-a..n all green). SHIP_CHECK=1 result at the foot.
8. **Grader**: `sim_e3` flatten param defaults 959 (unchanged for existing lanes), prevwap uses 565; new lanes appended to `res` dict and the wall line — pre-existing lane keys and ordering untouched; a day with zero bandpass/prevwap rows prints N=0 $+0.00 (dict pre-seeded :93). Not executed here (needs the live screener); shape read.
9. **Minor observations (not blocking):** (a) `_bp_in` uses wall-clock `datetime.now` at log time rather than the fire bar's k — on the deep rehydrate pass after a restart, old in-window fires re-detected minutes later could be stamped by the clock; with CONVERT=0 this only affects the in_window flag on replayed rows; on CONVERT=1 it prevents converting a stale rehydrated fire outside the window (safe direction) but could convert a rehydrated 09:31 fire at 09:35 restart — Pit Crew note: avoid RTH restarts (already law). (b) State maps are process memory — a restart forgets cooldown/episode; the day key handles date rollover.

FIX-NOW LIST: none. Docket (non-blocking): the `_bp_in`-by-clock nuance under rehydrate (9a) — surface if CONVERT is ever turned on; PRE quote stall exposure (finding 4) shared with the existing quote path.

## DAY-ONE WALKTHROUGH (Monday 8/17, default env, DRY_RUN=true)
- **07:00-09:25 ET**: `prevwap_shadow_fire` rows on names whose 10s closes reclaim the PRE-anchored line after ≥2 closes below, hold 12-30 closes, print a new minor high; each row carries `vwap`, `hold_n`, `hold_hi`, `crosses_20m`, `would_stop`, `spread_pct` (float or null when the quote is empty/timeout), `catalyst: null`, `time_hm`. Zero `triggered_*` rows from this lane, ever.
- **09:30-16:00 ET**: `bandpass_shadow_fire` rows all day with `in_window` true only 09:30-10:29 and `convert_on: false`; **zero `triggered_bandpass` and zero `bandpass_capped` rows** while CONVERT=0 — one such row = the guard regressed, pull the cord.
- **Boot**: banner shows `BANDPASS_SHADOW=1 BANDPASS_CONVERT=0(cap 3) PREVWAP_SHADOW=1`; `boot_config` row carries the four fields.
- **23:00 nightly**: the wall line gains `| bandpass(in-win) N=… $… (oow N=… $…) | prevwap(PRE,flat 09:25) N=… $…` — N=0 on a quiet day is a valid line, not an error.

## DOCTRINE-INVERSION SWEEP
doctrine-inversion sweep: n/a for the shipped default (observe-only rows; no gate/exempt/sizing rule changes). Under CONVERT=1 the lane obeys every existing gate (finding 6) — nothing inverts; the window restriction (in-window only converts) is the backtest's own edge boundary, not a new exemption.

## ROLL CALL (every ROSTER.txt office, 31)
- **Blast Radius Auditor** (chair): finding — convert path single-sited and env-gated (F1); PRE conversion-free by scan (F2); detector executed on 6 tapes (F3); the [UNVERIFIED] PRE-anchor question closed by read (F5).
- **Dashboard Curator**: finding — new statuses appear in decisions_archive; no dashboard tile ships with them yet (shadow-lanes board queue item stands); boot banner updated.
- **Systems Quant**: finding — `bandpass_step` computes what its name claims: reclaim (not bounce) via below_n, band-pass via streak bounds, chop via cross count; `hold_n` in the row = streak at fire (closes above before the fire bar).
- **Pit Crew Chief**: finding — kill switches `BANDPASS_SHADOW=0`, `PREVWAP_SHADOW=0`, `BANDPASS_CONVERT` already off; blocks wrapped in try; state is process memory (9b); no deploy here; flat-book-in-turn owed at deploy.
- **Integrator**: finding — both call sites enumerated (:7551 RTH, :7589 PRE), both consume the one `_nb`/`_vr_sv` built at :7439-7449; the grader is the only other consumer of the new statuses; boot row + banner wired.
- **Side Marshal**: clean — no side logic; rows can be joined to side stamps later.
- **Crown Steward**: clean — no crown privilege interaction; a crowned name's bandpass fire is a row like any other.
- **Feed Engineer**: finding — zero new bar fetches; PRE spread stamp = one capped (8 s) Webull snapshot per rare fire (F4).
- **Webull Broker Desk**: finding — the quote call goes through the existing pool/timeout path; no order semantics touched.
- **Quartermaster**: clean — no bars storage change; rows land in the decisions archive as usual.
- **Kev Librarian**: finding — Kev 8/10 "pullback to VWAP at 8:00" is the cited source; catalyst stamp is a TODO (null) — flagged for the corpus-side spec of "catalyst" before any PRE conversion is ever proposed.
- **First Hour**: finding — in_window flag = 09:30-10:29 = the paying cell from the 8/14 rebuild study; attribution can split in/oow from day one.
- **Opening Bell**: finding — the PRE lane is his window (07:00-09:25); rows only, 09:25 flatten in the grader.
- **Seam Scientist**: clean — not a beginning-entry lane; no registry change.
- **Strength Ombudsman**: clean — no refusal added; the shadow row logs every fire regardless of chop/cooldown? No — chop/cooldown suppress the row itself; noted: suppressed fires leave no row (a bias-ledger blind spot for later, not blocking).
- **Forward Architect**: finding — hypothesis registered: log suppressed (chop/cool) attempts as observe rows so the exclusions can be graded.
- **Momentum Operator**: clean — no noise ships; conversion is off.
- **Trade Manager**: finding — on convert, exit_mode E3, stop = hold low; unchanged exit machinery.
- **Tape Veteran**: hypothesis — hold_hi uses bar HIGHS, so a wick above the hold range fires the "new minor high" on a wick; the backtest used the same det_vwap mechanics per the header, so parity holds; recorded.
- **Reclaim Architect**: finding — the lane sits beside kev_reclaim_step on the same line; separate state, no interference; a name can fire both (rows only).
- **Execution Surgeon**: clean — no order-path change while CONVERT=0.
- **Handicapper**: clean.
- **Rocket Rider**: clean.
- **Cartographer**: clean — no map/level interaction; stop is structural (hold low), not a rung.
- **Wind Tunnel Engineer**: finding — grader flatten param threaded per lane; existing lanes unchanged at 959.
- **Statistician**: finding — wall line gains three columns; the archive query string extended; N=0 lines valid.
- **Convexity Trader**: clean — grading by $ mean via E3 sim, unchanged.
- **Curl Mechanic**: finding — fire-count acceptance for the new lanes starts Monday from the rows; window arg verified (AF-f).
- **Project Manager**: finding — tags: rig ALL GREEN incl. AF-a..n [VERIFIED]; PRE anchor [VERIFIED by read]; grader not executed live [UNVERIFIED shape-only]; CONVERT flip = Marcos's call, not taken here.
- **Historian**: finding — for the record: first lane whose PRE half is hard-coded shadow with no switch to convert.
- **Hidden Entry Architect**: clean — v2 shadow untouched; shares `_nb`/`_vr_sv` only.

## VERDICT
Room vote on "7ead311 is ship-clean as audited (shadow default-on = observe-only; BANDPASS_CONVERT default OFF with the single append env+window guarded, capped, non-exempt; PRE lane has no conversion path; detector executed against spec; zero new hot-path fetches; `_vr_sv` premarket-anchored so prevwap rows carry Kev's line; rig ALL GREEN incl. AF; no push/deploy from this convening)": **31-0 APPROVE, 0 blocking**. Flat-book-verified-in-turn owed at the moment of deploy. Turning BANDPASS_CONVERT on is not covered by this vote.

SHIP_CHECK result (run after the bookkeeping commit): see the chair's return message.

— Convening closed 2026-08-16 (clock cited above). Blast Radius Auditor, chair.

covers (post-audit bookkeeping): 457237f62951 — catalyst probe artifacts, data/killtests only, no code.
