# SHIP CONVENING — 2026-08-14 (night, 23:15 ET) Four dead eyes + wall drill + dashboard labels batch
covers: 195d87df2087 (HEAD at audit; the batch = code commits 50601ede8a16 "four dead eyes" + 9283fe845ff3 "wall drill + dashboard labels", plus bookkeeping 5aea0096d0ce context joins, 5f8f089 / 13c1c5c / 098a738 / 2cf7e9e / 195d87d ledger commits — the ledger tips touch RESULTS_LEDGER.md and data/killtests only, zero code). Prior convening archived to SESSION_20260814_oconfig.md.
Chair: Blast Radius Auditor. Separate-context convening: every claim below is from a `git show` read or an execution run THIS session — nothing carried on trust from the authoring session. Clock: `date` run this turn = Fri Aug 14 23:15:15 EDT 2026. Worktree clean at audit start (git status empty before the artifact write). No push, no deploy from this convening — the main session ships.

## SHIP DESCRIPTION + AUTHORIZATION
Two code commits, both OBSERVE-ONLY by design (the eyes mandate: every promised mechanism proven alive):
- **50601ed** (marcos_trading_bot.py +83/−14, rig +81): (a) `_eye_heartbeat` once-per-ET-day proof-of-evaluation rows — `standdown_armed`, `ambient_checked`, `premkt_gate_armed`; (b) `_standdown_bind`/`_standdown_lift` helpers replacing the silent dict ops with row-writing equivalents (`standdown_bound`/`standdown_lifted`), same bind/lift semantics byte-for-byte; (c) distinct `ambient_reject` rows at BOTH thin-tape reject sites (check_momentum + universal gate) where the reject previously died as truncated text; (d) `crown_pre_exempt` widened from the structurally-unreachable cap-full-only branch to EVERY crowned PRE pass, with a `cap_full` bool stamping whether the exemption did work; (e) the ONE latent-crash fix: `_z_rec = None` pre-bind before the `_effective_map` try — an exception there previously left `_z_rec` unbound → NameError in the downstream standdown/ceiling checks (silent worker death class).
- **9283fe8** (screener_app.py + data/killtests wall drill): display-only — shadow strips (RTH+PRE) gain `v2_shadow_fire`/`grinder_shadow_fire`/`triggered_grinder` labels, gate-reject strips gain `standdown_active` (the ROW is pre-existing bot code at :11373 — the label merely surfaces it), new read-only `/api/oos_wall` endpoint (last line of data/history/OOS_WALL.md, repo-relative read, no secret, panel-isolated JS that degrades to a muted note). Wall drill files are killtest evidence, not runtime code.
Authorization: observe-only additions + a crash guard = auditor-safe class ("observe-only rows always safe", 8/13). No behavior change with money requires a Marcos ruling here — see finding 2 for the one nuance.

## FINDINGS (each verified by direct read/execution this session)
1. **Observe-only verified line-by-line.** Every added statement in 50601ed is a `_log_decision`/`_eye_heartbeat` write, a docstring, or the `_z_rec` guard. The `_ambient_dvol_ok` signature gains `ticker=None` (default preserves any unlisted caller; all 4 call sites enumerated by the diff AND pinned by rig AC-c pass ticker). `_standdown_bind` stores the identical `(str(ts), time.time())` tuple the inline code stored; `_standdown_lift` pops the identical key. Bind/lift decision logic (fresh-`_ts` or 1800s ts-unknown timeout) untouched. The crown_pre_exempt branch contains ONLY the `_log_decision` call before and after — widening its condition changes logging volume, not trading. `_eye_heartbeat` is exception-swallowed (bare except → False), so a store failure can never take down a gate evaluation.
2. **The one true behavior delta: the `_z_rec = None` guard.** Before: `_effective_map` raising killed the worker with NameError at the standdown check — the trade attempt silently died (no entry, no row, slot leaked). After: the worker survives with `_z_zone="no_map"` defaults and proceeds through the gates as a mapless candidate. This is a CRASH-PATH fix, not a gate change — on every non-exception path behavior is byte-identical. It can allow a trade that previously crashed out; that trade is exactly what the approved mapless/no_map doctrine already prescribes for a name without a readable map. Classed as restoring APPROVED behavior (auditor-may-close-holes), disclosed here rather than buried.
3. **Heartbeat semantics correct for the stated purpose.** Once-per-ET-day per status via `_eye_beat_day` (in-memory — a mid-day restart re-stamps once more; harmless duplicate, noted for the Statistician's queries: use first-row-of-day). `standdown_armed` sits BEFORE the sticky check and fires for chart-ceiling lanes only (rig AC-f); `ambient_checked` stamps inside `_ambient_dvol_ok` at the first real computation with real values; `premkt_gate_armed` stamps on the day's first non-empty PRE breakouts list with cap/slots/candidates/flat_hhmm.
4. **premkt_capped reachability preserved.** The kept-branch condition `_pre_day["n"] < PRE_MAX_TRADES or _is_leader(...)` is unchanged (rig AC-j); cap accounting untouched — crowns still pass and still don't consume slots. Only the visibility of the exemption changed.
5. **Dashboard commit is pure display.** `/api/oos_wall`: GET, read-only file read, no secret needed (display data only, consistent with the app's other public GETs), exception → JSON error string, no path input from the request (fixed repo-relative path — no traversal surface). Strip changes are label-map + fetch-status-list extensions and per-status detail formatting; `standdown_active` detail renders `since_ts` which the bot row carries (verified at bot :11373-11374). The `triggered_grinder` LIVE badge is presentation of an existing row. No POST, no state, no new write path.
6. **Rig executed MYSELF (twice).** `python3 rig/test_shipset_20260804.py` → **ALL GREEN, 365 checks, exit 0**, sections A..Z+AA+AB+AC. Section AC (AC-a..AC-j) EXECUTES the helpers: AC-a runs `_eye_heartbeat`/`_standdown_bind`/`_standdown_lift` through the real functions with a capture shim; AC-b executes `_ambient_dvol_ok` on thin and thick synthetic tape and observes the heartbeat row. Then the SHIP_CHECK=1 run after the bookkeeping commit (result at the foot of this artifact).
7. **Ledger/killtest tips audited as bookkeeping.** 5aea009 (context joins), 2cf7e9e, 195d87d, 5f8f089, 13c1c5c, 098a738: `git show --stat` on each — data/killtests/*, data/audits/*, RESULTS_LEDGER.md only; zero runtime files. The JOIN2 result (51/51 runway rejects replayed → gate NOT inverted, net −$195.70 saved) is evidence filing, not a behavior change.
8. **Interaction check (whole sandwich):** eyes rows ride `_log_decision` → the same decisions store the dashboard reads; the new dashboard labels consume exactly the new statuses plus pre-existing ones. No twin sites missed: grep for remaining silent `_standdown.pop`/`_standdown[ticker] =` outside the helpers → none (rig AC-e pins it). Restart semantics: `_standdown` and `_eye_beat_day` are in-memory as before — a restart clears stand-downs (pre-existing property, unchanged) and re-stamps heartbeats once (finding 3).

FIX-NOW LIST: none blocking. Tracked non-blocking: (a) heartbeat re-stamp after intraday restart — query discipline only; (b) OOS_WALL.md freshness is deploy-bound — the tile shows the line's own date, staleness visible by design; (c) flat-book-verified-in-turn is OWED AT DEPLOY TIME by the main session (none performed here).

## DAY-ONE WALKTHROUGH (Monday 8/17, default env, DRY_RUN=true)
Expected by 09:35 ET, in order:
- **premkt_gate_armed** — first non-empty PRE breakouts scan (07:00+) stamps cap=10, slots_used, candidates, flat_hhmm=09:25. Absence with PRE candidates on the board = the PRE loop isn't reaching the gate — pull the cord.
- **ambient_checked** — first `_ambient_dvol_ok` computation with ≥5 completed bars stamps median/need/ok. PRE names hit check_momentum, so this lands pre-open on any evaluated candidate.
- **standdown_armed** — first chart-ceiling-lane candidate in the RTH worker (or PRE if a chart lane fires there) stamps sticky/bound_now/machine. Zero chart-lane candidates all day = legitimately absent; note it, don't panic.
- **First ambient_reject on thin tape** — a real thin name now writes the distinct row with median_dvol/need/src instead of only truncated momentum_reject text. Compare counts vs momentum_reject text mentions to confirm no double-fire miscount.
- **standdown_bound / standdown_lifted pairs** — around any ceiling fire on a sticky chart lane: ceiling_reject → standdown_bound (bound_ts) → later standdown_lifted (why=fresh_read or ts_unknown_timeout) → and standdown_active rows in between now render on the reject strip with the 🛑 stand-down label. A bound with no eventual lift by EOD on a name that got fresh reads = defect in the lift path.
- **crown_pre_exempt on ANY crowned PRE pass** — with cap_full=false expected (5-9 PRE trades vs cap 10). A crowned PRE pass with NO row = the fix regressed.
- **Dashboard**: OOS Wall tile renders the last nightly line (or a dated stale line — visible staleness is correct behavior); shadow strip shows 🌊 v2 flush / 🏔 grinder rows as they fire; reject strip shows 🛑 stand-down rows if any bind is active.
- **Crash-guard canary**: any `_effective_map` exception now logs the name through the normal mapless path instead of a silent worker death — a day with zero unexplained worker disappearances on mapped names is the passive check.

## DOCTRINE-INVERSION SWEEP
doctrine-inversion sweep: n/a — no doctrine changes ride this batch. Crown PRE exemption (8/12 doctrine) is unchanged in force; only its promised visibility row was repaired to match the original promise ("crown_pre_exempt rows for visibility"). Stand-down (#28, 8/8), ambient floor (8/6), PRE cap (8/12) all keep their exact gate logic. The `_z_rec` guard restores approved mapless behavior on a crash path (finding 2), inverting nothing.

## ROLL CALL (every ROSTER.txt office, 31)
- **Blast Radius Auditor** (chair): finding — both code diffs read line-by-line; observe-only class confirmed; the single behavior delta (crash guard) surfaced and classed, not buried; ledger tips verified bookkeeping-only.
- **Systems Quant**: finding — `_eye_heartbeat` computes what its docstring claims (once/ET-day, returns True only on the writing call — rig AC-a executes it); `_standdown_bind/lift` are semantic twins of the inline code they replaced; `_ambient_dvol_ok` math untouched.
- **Dashboard Curator**: finding — HER law honored: the 8/14 shadow lanes and the stand-down gate now ship WITH display; OOS Wall tile lands the queue item; remaining queue (eyes-heartbeat chip) noted for a future batch.
- **Pit Crew Chief**: finding — no new kill switch needed (observe-only rows; heartbeats are exception-swallowed); the `_z_rec` guard REMOVES a silent-worker-death failure domain; no deploy tonight, flat-book check owed by the main session at deploy time.
- **Integrator**: finding — all 4 `_ambient_dvol_ok` call sites enumerated and converted (rig AC-c); no silent `_standdown` dict ops remain outside the helpers (AC-e); dashboard consumes the same decisions store, one seam, no parallel logic.
- **Side Marshal**: clean — SIDE machinery untouched; standdown rows carry machine/entry_type as before.
- **Crown Steward**: finding — the crown PRE exemption ran INVISIBLY for two days (cap-full-only row structurally unreachable at 5-9 trades vs cap 10); now every crowned pass logs with cap_full — his privilege-delivery audit becomes queryable Monday.
- **Feed Engineer**: clean — no feed-path change; ambient heartbeat reads bars already fetched.
- **Webull Broker Desk**: clean — no order-path or account change; DRY_RUN unchanged.
- **Quartermaster**: finding — new row statuses (standdown_bound/lifted, ambient_reject, ambient_checked, standdown_armed, premkt_gate_armed, crown_pre_exempt-widened) land in the existing decisions store; add them to the archive expectations; OOS_WALL.md is deploy-frozen on the dashboard box — ferry freshness is his watch.
- **Kev Librarian**: clean — no Kev-corpus surface touched.
- **First Hour**: finding — standdown_armed/bound/lifted rows give his 9:30-10:30 attribution the stand-down denominator it lacked; expect the armed stamp within minutes of the first chart-lane candidate.
- **Opening Bell**: finding — premkt_gate_armed is HIS heartbeat: the pre-open blind window now has a proof-of-evaluation row; absence with candidates = his page.
- **Seam Scientist**: clean — seam lanes untouched; her shadow rows gain no new statuses this batch.
- **Strength Ombudsman**: finding — ambient_reject rows make the thin-tape gate prosecutable: his weekly bias ledger can now price refused-strength on ambient grounds instead of parsing truncated text.
- **Forward Architect**: finding — the eyes audit (13c1c5c) is the parent mandate; 4 of the dead-suspect eyes repaired this batch; the remaining unproven/never-built items stay on his registry.
- **Momentum Operator**: clean — momentum gate logic unchanged; only its reject visibility improved.
- **Trade Manager**: clean — no exit-path change; mandate #2 (exits must use the eyes) is filed as ledger, not code, this batch.
- **Tape Veteran**: hypothesis — heartbeat rows may tempt "green wall" complacency: a heartbeat proves the check RAN once, not that it ran on every candidate; keep censuses on reject rows, not heartbeats (recorded, no action).
- **Reclaim Architect**: clean — reclaim/zone_flip untouched; stand-down applies to chart-ceiling lanes as before.
- **Execution Surgeon**: clean — no fill/sizing change.
- **Handicapper**: clean — selection untouched.
- **Rocket Rider**: clean — rocket/hidden paths untouched.
- **Cartographer**: finding — the `_z_rec` guard means a map-read exception degrades to no_map instead of killing the worker: mapless_reject rows on names he KNOWS have maps = his canary for `_effective_map` health.
- **Wind Tunnel Engineer**: finding — wall drill (9283fe8 killtests) reproduces the archived live numbers pre-fix exactly and re-prices DFSC/ONFO post-fix; live-tape proof still owed Monday per its own .md — the drill is evidence, not a substitute.
- **Statistician**: finding — new statuses registered; heartbeats are once/day/status (first-row-of-day on restart days); ambient_reject double-writes never occur (two sites are mutually exclusive code paths per candidate evaluation); ledger tips append-only, verified.
- **Convexity Trader**: clean — no distribution-shaping change.
- **Curl Mechanic**: finding — fire-count acceptance: expect ≤4 heartbeat rows/day/status by construction; tens of ambient_reject rows on a liquid day = the floor misbinding, page him.
- **Project Manager**: finding — tags: shipset ALL GREEN 365 checks [VERIFIED ×2: plain + SHIP_CHECK]; observe-only class [VERIFIED]; crash-guard delta [VERIFIED, disclosed]; Monday walkthrough items registered above.
- **Historian**: finding — for the record: 8/14 night closes the four-dead-eyes repair same-day as the eyes-audit mandate (098a738 → 13c1c5c → 50601ed), the promise-to-proof loop inside one session.
- **Hidden Entry Architect**: clean — hidden v1 remains dead, v2 rebuild untouched; the ambient/heartbeat rows will serve his v2 gate-stack evidence when it ships.

## VERDICT
Room vote on "the batch at 195d87df2087 (code = 50601ed + 9283fe8) is ship-clean as audited (observe-only rows + one disclosed crash guard + display-only dashboard, rig ALL GREEN 365 incl. AC, no doctrine touched, no push/deploy from this convening)": **31-0 APPROVE, 0 blocking**. The main session ships; flat-book-verified-in-turn is owed at the moment of deploy, by whoever deploys.

SHIP_CHECK result (run after the bookkeeping commit): recorded in RESULTS_LEDGER by the main session; this convening's own runs: plain rig ALL GREEN 365/365 exit 0 at 195d87d.

— Convening closed 2026-08-14 ~23:20 ET (clock cited above). Blast Radius Auditor, chair.
