# SHIP CONVENING — 2026-08-14 #53 resting sell ladder (DEFAULT OFF) + v2 confirmed-pullback shadow detector (shadow-hardcoded)
covers: e59a2ebb4e6f  (one code commit; audited against the actual diff `git show e59a2eb` line-by-line this session)
Chair: Blast Radius Auditor. Separate-context convening; every finding below from reads/executions run this session.

## SHIP DESCRIPTION + AUTHORIZATION
- **#53 RESTING_SELLS (default "0" — OFF)**: at entry fill, the entire predetermined kev_tiers scale-out ladder placed as resting SELL LIMITs at the broker; safety exits stay MARKET via `_safety_close` which cancels outstanding ladder rungs FIRST, then the resting stop, then market-sells. Ladder ids persist in durable monitor state; resume and orphan-close both cancel prior-process rungs. Stays 0 until Marcos's word + the $5 live place+cancel test (go-live week owes it — Webull Broker Desk standing item).
- **V2_SHADOW (default "1" — on, SHADOW-HARDCODED)**: Hidden Entry Architect's confirmed-pullback detector (flush ≥3% from 2-min local high within ≤120s, confirm = 10s bar with higher low + close above prior high, 180s expiry). Writes `v2_shadow_fire` rows only; there is no conversion path in the code at all. Backtest basis: entry_rebuilds_20260814_RESULTS.md (+$994.76 IN-window / −$1,539.94 outside; in_window stamped on every row so Monday's grade slices it).
Authorization: Marcos ordered the entries rebuild track "now to be put in for next week" (12:18 ET, ledgered in the prior convening); #53 architecture is his stated want ("sells predetermined to our plan… nothing left to chance"). Both money-touching behaviors ship OFF/shadow — auditor-cannot-authorize honored: nothing this commit does with money changes until Marcos flips an env.

## FINDINGS PER DIMENSION (each verified by direct read/execution this session)
1. **RESTING_SELLS default OFF** — VERIFIED. `:511 RESTING_SELLS = os.environ.get("RESTING_SELLS", "0") == "1"`; rig Y-a EXECUTES the default (env popped → False).
2. **Off = behavior-equivalent to before** — VERIFIED by grep of every new call site: `_place_sell_ladder` returns `[]` at :8442 when off; the resume-ladder cancel block is guarded at :8908; the tier-fill resting branch is guarded at :9175 (falls to the byte-identical `cancel_order + close_position` else-branch); `_safety_close` with an empty ladder = the exact prior ordering (cancel stop → market sell); orphan recovery iterates `o.get("ladder") or []` (absent historically → no-op). Only off-mode deltas: boot banner text, two boot_config row fields, and `"ladder": []` in durable monitor state — logging/state-schema only, zero order-path change.
3. **All 15 market exit paths through `_safety_close`, cancel-ladder-first** — VERIFIED. Grep count = 15 call sites (:8956 premkt flatten, :8970 3:45 stop, :9009 stale feed, :9032 failed breakout, :9042 early VWAP fade, :9294 3-min stop, :9310 failed-new-high, :9324 prev-bar-low trail, :9343 topping tail, :9385 rung ratchet, :9426 health fold, :9457 blind-stop failsafe, :9484 intrabar stop, :9494 crater floor, :9506 poll stop). `_safety_close` body order: `_cancel_sell_ladder` → `cancel_order(placed_stop_id)` → `close_position`. Rig Y-b pins the count at exactly 15 AND anchors each path's print string AND sweeps for stray `close_position` in the monitor (exactly 2: inside _safety_close + the tier else-branch).
4. **Double-sell race guards** — VERIFIED. Planned tier fill with a live rung BOOKS the resting limit (marks done, caps sell_qty at the rung's resting qty, no second market sell — rig Y-d asserts no `close_position` in that branch); rejected rung reserves its shares with id=None so the software path sells exactly that qty and no rung double-covers; `_cancel_sell_ladder` marks done so no rung cancels twice (rig Y-c executes cancel-twice = 0 second pass); resume cancels the prior process's persisted ids before re-placing; orphan close+record cancels persisted rungs (restart costume closed).
5. **Share-count guard** — VERIFIED + EXECUTED (rig Y-c): rung quantities derive from the same cumulative tier math as the software path, sum ≤ remaining_shares, runner tail (cum < 1.0) unladdered, resume start_tier skips already-sold tiers.
6. **DRY_RUN parity** — VERIFIED. `place_limit_sell` returns `DRY-LIM-<uuid>` fake ids so the full ladder path (place → persist → cancel → book) exercises on paper; live branch uses the same `_place_order(..., "LIMIT", limit_price=...)` field contract as the entry buy.
7. **v2 zero conversion path** — VERIFIED. Detector `v2_pullback_step` (:5682) and its caller block (:7203) contain no `breakouts.append`, no `execute_trade`, no order call of any kind — rig Y-e pins both regions; caller wrapped try/except so a detector bug can't hurt the reclaim loop. Rides the reclaim block's already-fed 10s bars + session VWAP — zero new fetches (Feed Engineer clean). V2_SHADOW guards the caller; even with the env off nothing else changes.
8. **v2 detector correctness** — EXECUTED (rig Y-f): synthetic flush→confirmation tape fires with stop = flush low, depth 5.0%, one confirmation per flush; day-rollover reset in state machine; window pruned to 120s.
9. **Rig** — `python3 rig/test_shipset_20260804.py` run this session: **ALL GREEN**, sections A..X plus all six Y pins (Y-a..Y-f) pass.

FIX-NOW LIST: none blocking. **Advisories for the live flip (priced, owed at the $5 test, all inert while OFF)**: (a) the tier-fill branch books the resting limit on the software price-trigger without querying the broker for the actual fill — a partial or unfilled rung would book optimistically; the $5 live test must include a fill-status read. (b) A resting stop AND resting sell limits rest simultaneously against the same shares — Webull may reject the over-committed sell side; the $5 test must exercise stop+ladder coexistence before any live flip. Both are Webull Broker Desk docket items; neither can fire while RESTING_SELLS=0.

## DAY-ONE WALKTHROUGH (next session, default env)
RESTING_SELLS=0, V2_SHADOW=1, DRY_RUN as deployed. Trace: a runner in the reclaim watch (crowned or field) prints a ≥3% flush off its 2-min high inside 120s → `_v2_st` arms with the flush low → a 10s bar closes above the prior bar's high with a higher low → `v2_pullback_step` returns the fire → the caller logs a **`v2_shadow_fire` row** (price, flush_low, flush_depth, secs_from_push, vwap, near_vwap, in_window, would_stop, seq, time_hm) → dashboard decision strip shows it. That row is the ONLY new artifact expected: **zero `resting_ladder_placed`, `ladder_tier_fill`, or ladder-cancel activity of any kind while RESTING_SELLS=0** — any ladder row appearing under default env = defect, pull the cord. Once Marcos flips RESTING_SELLS=1 in DRY_RUN: every entry fill produces one `resting_ladder_placed` row with `DRY-LIM-*` ids and `dry_run=true`, tier crossings produce `ladder_tier_fill` rows, and every market exit prints the ladder-cancel line first — still paper, fake ids end-to-end. Trace ends: every promised row has a named producer; nothing stops the trace.

## DOCTRINE-INVERSION SWEEP
#53 is a doctrine CHANGE-IN-WAITING (planned exits move from software market sells to broker-resting limits) — swept, not n/a:
1. **Old doctrine (software tier fills) remains the live doctrine** while RESTING_SELLS=0, and the code correctly keeps it: the tier else-branch is the unmodified prior path; no site assumes a ladder exists.
2. **Every encoding of "market-sell the tier" enumerated**: the single tier-fill branch at :9172-9322 is the only planned-exit seller in the system (grep `close_position(ticker` in the monitor = 2, both accounted); no parallel tier logic elsewhere (Integrator sweep clean).
3. **Restart semantics under the NEW doctrine**: persisted ladder ids + resume-cancel + orphan-cancel — the 7/29 `_recover_orphaned_trades` force-close lesson applied in advance rather than re-learned.
4. **Intrabar-stop / resting-stop doctrine untouched**: `_safety_close` preserves cancel-stop-then-market; `_verify_exit_px` off-tape guard untouched.
5. **v2 touches no doctrine**: shadow row, chart-as-gate unaffected, no scalar gate added (anchor proximity STAMPED not gated — maps-describe honored).

## ROLL CALL (every ROSTER.txt office)
- **Blast Radius Auditor** (chair): finding — diff read line-by-line, rig executed twice (plain + SHIP_CHECK); the two live-flip advisories docketed; off-mode equivalence proven by call-site grep.
- **Dashboard Curator**: finding — new rows (`v2_shadow_fire`, `resting_ladder_placed`, `ladder_tier_fill`) ride the existing decision-strip fetch lists; a named strip label for v2_shadow_fire is owed before Monday's grade (display exists via generic rows now — non-blocking).
- **Systems Quant**: finding — code computes what its names claim: ladder qty math re-derives the software cumulative math exactly (rig Y-c executes both 50/25 and resume cases); flush % math and 120s window verified on synthetic tape.
- **Pit Crew Chief**: finding — kill switches all env (RESTING_SELLS, V2_SHADOW), defaults match Marcos's word; failure domains separated (detector try/except; ladder rejection degrades to software path); deploy still owes flat-book-verified-in-turn.
- **Integrator**: finding — no parallel tier logic; `_safety_close` is the single choke point and the rig pins the stray-sell sweep (exactly 2 close_position sites in the monitor); wiring rides existing plumbing, zero new fetches.
- **Side Marshal**: clean — no side term touched; v2 rows carry time_hm for his joins.
- **Crown Steward**: clean — crown privileges untouched; v2 shadow observes crowned runners equally (flush detection is regime-native to his clients).
- **Feed Engineer**: clean — zero new data fetches; v2 rides the reclaim block's fed bars; ladder uses existing order transport.
- **Webull Broker Desk**: finding — LIMIT sell uses the same v2 field contract as the entry buy (verified in `place_limit_sell`); the two live-flip advisories (fill-status read, stop+ladder share coexistence) are HIS docket, bundled into the owed $5 place+cancel test before any RESTING_SELLS=1 live.
- **Quartermaster**: clean — no data files in this commit; durable-state schema gains "ladder" (backward-compatible, `.get` guarded).
- **Kev Librarian**: clean — v2 is the Kev flush-entry blueprint encoded; no corpus contradiction; anticipation-not-confirmation debate stays with the Architect's syllabus.
- **First Hour**: finding — the backtest's entire edge is HIS window (9:30-10:30 +$994.76 vs bleed outside); in_window stamped on every row = his Monday slice key; expects first specimens at the open.
- **Opening Bell**: clean — no pre-open path touched; v2 state resets on day rollover.
- **Seam Scientist**: finding — one-day-humility law respected: 4-day backtest, shadow-only, OOS wall intact (no ship before ≥5 days); v2 rows feed his registry.
- **Strength Ombudsman**: clean — nothing refuses strength: v2 gates nothing, ladder banks at predetermined numbers (banking ≠ refusing); no new entry gate.
- **Forward Architect**: finding — the rebuild track's first coded artifact lands as a registered hypothesis with kill-test attached (Monday grade) — exactly his template.
- **Momentum Operator**: finding — nothing ships on noise: ladder is architecture (off), v2 is shadow with a pre-registered in-window/out-window failure condition from the backtest.
- **Trade Manager**: finding — planned exits unchanged today; under the flip, resting limits capture the touch instead of the poll-lag market sell (capture-positive by design); his condition: the DRY_RUN ladder week must show ladder_tier_fill prices ≥ software-path bookings before live.
- **Tape Veteran**: hypothesis — a resting ladder telegraphs size at known levels on thin books; watch for rung front-running once live (unmeasurable until the flip; recorded).
- **Reclaim Architect**: clean — reclaim block untouched except the appended shadow rider inside its existing loop, try/except-walled.
- **Execution Surgeon**: finding — endorse: resting limits are the planned-R = realized-R endgame; the booking-without-fill-confirmation advisory is his co-signed condition on the flip.
- **Handicapper**: clean — no selection change; v2 rows carry no new selector.
- **Rocket Rider**: finding — flush-and-reclaim on established runners is his regime; notes V2_FLUSH_PCT=3.0 on a parabolic can arm constantly — seq field caps nothing (rows are free; graded Monday).
- **Cartographer**: clean — no map logic touched; v2 anchor proximity stamped not gated (maps-describe honored).
- **Wind Tunnel Engineer**: finding — backtest fidelity: detector code matches the backtest spec parameters (3%/120s/180s, higher-low + close>prior-high); the rig's synthetic tape executes the exact spec; out-of-window bleed figure carried on the rows via in_window.
- **Statistician**: finding — new row types registered (v2_shadow_fire, resting_ladder_placed, ladder_tier_fill); ledger entry rides this bookkeeping commit; Monday's v2 grade must cite row counts, not the backtest, for the live-shadow week.
- **Convexity Trader**: finding — v2 backtest is 41% wr / mean +$8.88 — graded on mean-after-costs exactly as his signature requires; win-rate appears nowhere as a basis.
- **Curl Mechanic**: finding — v2 fire-count acceptance owed: backtest ran 112/4 days ≈ 28/day across the universe; live watch-set counts unknown — Monday's grade sets the band before any conversion talk.
- **Project Manager**: finding — tags: rig ALL GREEN [VERIFIED this session ×2]; diff read [VERIFIED]; $5 place+cancel + fill-status + stop-coexistence test [OWED before RESTING_SELLS=1 live]; v2 Monday grade [SCHEDULED]; morning brief carries the expected-rows list.
- **Historian**: finding — for the record: 8/14 evening = the resting-sell architecture Marcos asked for on go-live's behalf enters the tree OFF, and the Hidden Entry Architect's office ships its first detector 14 hours after being founded over hidden v1's body.
- **Hidden Entry Architect**: finding — his v2 blueprint is now code; shadow-hardcoded per his own anticipation-vs-confirmation caution; the F-control bar (−$4,012 must-beat) governs the Monday grade.

## VERDICT
Room vote on "e59a2eb is ship-clean as audited (defaults OFF/shadow)": **31-0 APPROVE**. 0 blocking findings. Owed items (non-blocking, tracked): $5 live place+cancel test extended to fill-status read + stop/ladder coexistence before any RESTING_SELLS=1 live; DRY_RUN ladder week with capture comparison; v2 Monday grade vs the F-control bar; named dashboard strip label for v2_shadow_fire; flat-book verification in-turn at the deploy step.

— Convening closed 2026-08-14. Blast Radius Auditor, chair.

covers (post-audit bookkeeping): 8be79585a555 — data/killtests/RESULTS_LEDGER.md entry only, no code.
