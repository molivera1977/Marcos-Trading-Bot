# SHIP CONVENING — 2026-08-17 (Sunday night → Monday, 00:20 ET) TWO PRE SIM-CONVERSION PATHS: PRE-VWAP reclaim lane "prevwap" (PREVWAP_CONVERT env-OFF) + v2 quiet-tape flush lane "v2conv" (V2_CONVERT/V2_QUIET_ONLY env-OFF, CAUSAL trailing-calm gate) + rig AH (13 checks) + refreshed pins (AF-i, Y-e, Z-d)
covers: 197ed0d6f817 "build: v2 quiet-tape convert gate (causal trailing calm) — PRE sim, env-off" — ONE batch, three files: marcos_trading_bot.py (v2conv convert path + CAUSAL v2_trailing_calm gate AND the swept-in prevwap convert path; +15 prevwap lines that were 0 at HEAD~1 = both PRE conversions ship together), data/killtests/nightly_shadow_grade.py (triggered_v2conv grader status), rig/test_shipset_20260804.py (new AH section a/i-xiii + refreshed AF-i/Y-e/Z-d). Prior audited code: 5e77993 (kevseq, convening archived SESSION_20260816_kevseq via LATEST). Bookkeeping since: db8a425, f34d9f4 (data/audits only, no code).
Chair: Blast Radius Auditor. Separate-context FINAL ship convening. Every claim below is from a `git show`/file read or an execution run THIS session. Clock: `date` run this turn = Mon Aug 17 00:14:24 EDT 2026 (Monday premarket boot ~03:55 ET; ~3h40m of runway). No push, no deploy, no env flip from this convening — the main session ships and flips envs.

## SHIP DESCRIPTION + AUTHORIZATION
Two PRE sim-conversion paths, both **env-OFF by default**, both ALL SIM (DRY_RUN stays true). Marcos's order: "have them all pre-market live" — the lanes become *eligible to fire in premarket* under their convert flags; no real dollars move.
- **prevwap** (PRE-VWAP reclaim): envs `PREVWAP_CONVERT` default "0" (:5954). Detector = `bandpass_step(..., 420, 565)` (the 7-9.4 min band-pass reclaim, :7936). Caller in the reclaim block of `wait_for_flat_top_entry` (:7935-7973), AFTER the kevseq block. On convert: lane "prevwap", stop=would_stop (hold low), exit_mode E3, session "PRE" (09:25 flatten). Rows: `prevwap_shadow_fire` always (stamps spread_pct + catalyst + convert_on), `triggered_prevwap` on convert.
- **v2conv** (v2 confirmed-pullback flush on quiet tape): envs `V2_CONVERT` default "0" (:5766), `V2_QUIET_ONLY` default "1" (:5767), `V2_DAILY_CAP` 5 (:5768), `V2_QUIET_BPS` 89.9 (:5769), `V2_QUIET_LOOK` 30 (:5770), `V2_QUIET_MINB` 10 (:5771). Detector `v2_pullback_step` (unchanged from the 8/14 shadow). New CAUSAL gate `v2_trailing_calm(sym, fire_k)` (:5776-5791). Caller in the reclaim block (:7711-7773) under `if V2_SHADOW or V2_CONVERT:`. On convert: lane "v2conv", stop=would_stop (flush low), E3, session PRE(<09:30, 09:25 flatten)/RTH, cap 5/day. Rows: `v2_shadow_fire` under V2_SHADOW, `v2conv_capped` on cap hit, breakouts.append on convert.
Authorization class: shadow rows = observe-only (always safe). BOTH conversion paths are behavior-changes-WITH-MONEY that ship **DEFAULT OFF**; flipping `PREVWAP_CONVERT=1` and/or `V2_CONVERT=1` is Marcos's call (his explicit "have them all pre-market live" order, executed by the main session AFTER this audit) and the sim exposure it authorizes = SIM rows only while DRY_RUN=true.

## FINDINGS (each verified by direct read/execution this session)
1. **Both convert paths default OFF; zero reachable appends with them off.** `os.environ.get("PREVWAP_CONVERT","0")=="1"` (:5954), `os.environ.get("V2_CONVERT","0")=="1"` (:5766). grep of the two lane strings: the ONLY `breakouts.append(... "prevwap" ...)` is :7964 under `if PREVWAP_CONVERT and _pvf["would_stop"] < _pvf["px"]:` (:7959); the ONLY `breakouts.append(... "v2conv" ...)` is :7763 under `if (V2_CONVERT and (not V2_QUIET_ONLY or _v2_quiet) and _v2f["would_stop"] < _v2f["px"]):` (:7747-7748). Rig AH-vii executed the v2 guard with convert off → `_bo == []`; AF-i asserts the prevwap guard precedes its append. Both detectors are conversion-free (AH-iv: no append/execute in the detector/helper segment). Rig run by me: ALL GREEN, exit 0, 419 checks.
2. **The v2 quiet gate is CAUSAL — no future bar is ever read.** `v2_trailing_calm` (:5782-5791) builds `prior = [b for b in hist if b[0] < fire_k][-V2_QUIET_LOOK:]` — bars STRICTLY BEFORE the fire bar (`b[0] < fire_k`); bars at `k == fire_k` and `k > fire_k` are excluded by construction. Buffer maintenance (:7717-7721) appends this call's completed bars `_nb` to `_v2_hist[t]` and trims to 90 BEFORE `v2_pullback_step` runs, so the metric never has to reach forward. `< V2_QUIET_MINB` prior bars → `(None, n)` → caller treats as NOT quiet (:5784-5785, :7727). Rig AH-ii ("trailing metric reads ONLY bars strictly before fire_k; k==fire and k>fire excluded") and AH-vi ("causal buffer maintained every call + bounded, metric keyed on fire_k") both GREEN. **Causality CONFIRMED.**
3. **PRE_LANES additions ride the convert envs.** `PRE_LANES` base = {hidden_entry, vwap_reclaim} (:12936). `PRE_LANES.add("prevwap")` is inside `if PREVWAP_CONVERT:` (:12940-12941); `PRE_LANES.add("v2conv")` inside `if V2_CONVERT:` (:12945-12946). Default-off → PRE_LANES is untouched (exactly the prior set). Rig AH-ix asserts the v2conv add is gated by `if V2_CONVERT:`.
4. **Neither lane in any exempt set; both hit the PRE cap + 09:25 flatten.** grepped all four: `MIN_STOP_EXEMPT` (:7362, {zone_flip,hidden_entry,flat_top}), `BACKSIDE_EXEMPT` (:7082, {dip_rip}), `VRIDE_EXEMPT` (:492, {hidden_entry}), `_STALE_EXEMPT` (:3274, {rocket_catcher,vwap_reclaim,zone_flip,hidden_entry}) — NONE contains "prevwap" or "v2conv" (rig AH-x for v2conv). Converted fires walk the full gate stack, stop delivered as `zone_stop`=would_stop. PRE guardrails: `PRE_MAX_TRADES=6` (:12950), `PRE_FLAT_HHMM=09:25` (:12952) apply to both since both are PRE lanes on convert; v2conv additionally capped at `V2_DAILY_CAP=5`/day (:7752, `v2conv_capped` row on hit — rig AH-xi).
5. **The swept-in prevwap work is coherent with the v2 build (ONE batch).** `git show 197ed0d` = the sole commit; PREVWAP_CONVERT had 0 hits at HEAD~1 (4cc00ae) and 15 added `+` lines in this diff → the prevwap conversion path was authored and shipped in the SAME commit as v2conv. Both follow the identical shape: env-gated single append, E3, would_stop, PRE session, non-exempt, convert_on stamped on the shadow row. No half-applied state; `git status` clean before my LATEST.md write.
6. **Rig executed MYSELF**: `python3 rig/test_shipset_20260804.py` → **ALL GREEN, exit 0, 419 ✅ / 0 ❌**, including the new **AH section** (a, i-xiii — all 13 green: envs default, calm passes / busy blocks, causal read, conversion-free detector, guarded append, bounded buffer, convert=0→zero appends, quiet_only semantics, PRE_LANES gating, exempt-set exclusion, daily cap, grader, boot stamp) and the refreshed pins **AF-i** (prevwap converts ONLY under PREVWAP_CONVERT), **Y-e / Z-d** (v2 detector conversion-free, caller guarded). Standing pin sections G/H/I/L/M/N/O all green.

## SPEC TENSIONS (builder-flagged; Marcos-ACKNOWLEDGED, NOT blockers)
- **Deviation #1 (causal vs descriptive window)**: the joint_door study's calm was a DESCRIPTIVE 09:00-09:30 (or 07:30-08:00) median-range window — lookahead for a 07:00-09:00 fire. The live gate deliberately does NOT replicate it; it uses a CAUSAL trailing metric (last 30 completed 10s bars strictly before fire_k). Documented in the block comment (:5758-5765) and the commit body. Marcos-acknowledged design choice, stamped for grading.
- **Deviation #2 (10s vs 1-min metric)**: study measured median range/close on 1-MIN bars; the live gate measures on 10s bars. 10s ranges run smaller than 1-min ranges, so **89.9 bps is a GENEROUS ceiling** on the 10s metric — it will admit tape the 1-min threshold would too. `quiet_tape` bool + `quiet_bps` metric are STAMPED on every fire row so the threshold recalibrates from real rows without a code change (:5762-5765). The `quiet_bps` threshold is **observational for week-1** — a stamp, not yet a proven cutoff. Marcos-acknowledged; recalibration owed from Monday+ rows.

FIX-NOW LIST: none.

## DAY-ONE WALKTHROUGH (Monday 8/17, DEFAULT env, DRY_RUN=true)
Note: at the moment of this audit both convert envs are OFF. The main session flips `PREVWAP_CONVERT=1` and `V2_CONVERT=1` (Marcos's "have them all pre-market live" order) AFTER shipping. The walkthrough below is the state Marcos will run once flipped — ALL SIM, zero real dollars.
- **Boot** (~03:55 ET): banner stamps `PREVWAP_CONVERT`, `V2_CONVERT=…(quiet_only 1, <=89.9bps, cap 5)`; `boot_config` row carries prevwap_convert, v2_convert, v2_quiet_only, v2_daily_cap. With the flags flipped, PRE_LANES = {hidden_entry, vwap_reclaim, prevwap, v2conv}.
- **07:00-09:25 (PRE window)**: prevwap fires (band-pass reclaim) → `prevwap_shadow_fire` then `triggered_prevwap` (lane "prevwap", E3, stop=hold low, session PRE), stamping spread_pct + hold_n + crosses_20m. v2 flushes on quiet-premarket-tape names → `v2_shadow_fire` then, if the CAUSAL quiet gate passes (quiet_bps<=89.9) and cap not hit, the 🤫 convert → lane "v2conv", E3, stop=flush low, session PRE, `#n/5`. kevseq shadow/reject rows continue (unchanged, still CONVERT-gated separately). Every PRE fire carries quiet_tape / spread_pct / seq stamps.
- **09:25 flatten**: PRE_FLAT_HHMM=09:25 flattens all PRE-session positions before the 09:30 open (prevwap and v2conv PRE fires included) — no PRE trade crosses into RTH.
- **09:30-10:00**: v2conv can also fire RTH (session "RTH", same causal gate, shares the 5/day cap). prevwap is PRE-only (band-pass window 07:00-09:25).
- **Cap / kill**: v2conv stops appending at 5/day (`v2conv_capped` rows thereafter). Kill either lane instantly by flipping its env back to 0.
- **DRY_RUN=true throughout — zero real dollars.** The `quiet_bps` threshold is observational week-1; grade the stamped rows before trusting the 89.9bps cutoff.

## DOCTRINE-INVERSION SWEEP
doctrine-inversion sweep: **n/a** for the shipped tree. Both lanes are shadow rows (observe-only) with the conversion paths env-OFF by default; when flipped they are ALL SIM (DRY_RUN true), non-exempt (full gate stack), PRE-capped, 09:25-flattened, and read no future bar. No existing gate, exempt set, sizing rule, or exit machinery is altered — the v2 quiet gate is an ADDITIONAL front-side/not-yet-extended filter (a cousin of the back-side gate), it loosens nothing. No doctrine is inverted.

## ROLL CALL (every ROSTER.txt office, 31)
- **Blast Radius Auditor** (chair): finding — two single-sited env-guarded appends (F1); CAUSAL gate proven by read + rig AH-ii/vi (F2); PRE_LANES rides the flags (F3); non-exempt + PRE-capped + flattened (F4); one coherent batch (F5); rig ALL GREEN incl. AH executed here (F6).
- **Dashboard Curator**: finding — new shadow rows (prevwap_shadow_fire stamps spread_pct+catalyst; v2 stamps quiet_tape+quiet_bps+quiet_n+convert_on); no new tile this batch — shadow-lanes board queue still stands.
- **Systems Quant**: finding — `v2_trailing_calm` computes what its name claims (median 10s range/close bps over prior bars); the `< fire_k` filter is the whole causality guarantee and is present + tested.
- **Pit Crew Chief**: finding — kill switches = the two convert envs (flip to 0); both blocks wrapped in try/except; `_v2_hist`/`_v2_conv_day` are process-memory (restart resets the day counter and buffer — day key reset is intentional); no deploy here; flat-book-in-turn owed at the moment of deploy.
- **Integrator**: finding — prevwap + v2conv each = one call site inside the existing reclaim block; consumers of new statuses = the grader only (triggered_v2conv added); `_nb`/`_vr_sv` shared with hidden/kevseq, no new fetch.
- **Side Marshal**: finding — v2 quiet gate is a front-side/not-yet-extended filter (his territory); quiet_tape stamped on every fire, joinable to the side ledger.
- **Crown Steward**: clean — no crown privilege interaction; crowned names produce rows like any other.
- **Feed Engineer**: finding — zero new bar/quote fetches; prevwap adds one `_get_webull_quote` for the spread stamp inside its own try (fail-soft to None).
- **Webull Broker Desk**: clean — no order semantics touched; DRY_RUN true, sim only.
- **Quartermaster**: clean — no new persistent artifact beyond decision rows; buffers are in-memory.
- **Kev Librarian**: finding — neither lane is Kev's fingerprint (both are front-side selection filters, explicitly noted in the header); catalyst still null on prevwap.
- **First Hour**: finding — both lanes carry time_hm; PRE + first-30-min attribution available Monday.
- **Opening Bell**: finding — this is the PRE-hours ship: prevwap 07:00-09:25, v2conv 07:00-09:30 PRE then RTH; 09:25 flatten honored (F4).
- **Seam Scientist**: finding — v2 flush is a beginning-entry lane on the fast chart; one-day humility + OOS wall apply; quiet_bps is observational week-1.
- **Strength Ombudsman**: finding — the quiet gate REFUSES busy/extended tape (not weakness) — a front-side filter; refusals will show as non-converted shadow rows for the bias ledger.
- **Forward Architect**: finding — Deviation #1/#2 are registered hypotheses with the stamped metric as the kill-test instrument.
- **Momentum Operator**: clean — nothing ships on noise; both converts env-OFF at ship.
- **Trade Manager**: finding — on convert both are E3; prevwap stop=hold low, v2conv stop=flush low; unchanged exit machinery.
- **Tape Veteran**: hypothesis — 10s median-range calm is a generous proxy for the 1-min study calm; rows will show whether the two agree — recorded.
- **Reclaim Architect**: finding — prevwap rides the band-pass reclaim detector (7-9.4min band); separate state from kev_reclaim; same fed bars.
- **Execution Surgeon**: clean — no order path change; DRY_RUN true.
- **Handicapper**: finding — day-gain/selection unchanged; the quiet gate is a tradeability/front-side floor, not a setup-quality scalar.
- **Rocket Rider**: clean — parabolic regime untouched; flush is a pullback lane.
- **Cartographer**: clean — stops are structural (hold low / flush low), no rungs.
- **Wind Tunnel Engineer**: finding — grader gains triggered_v2conv (E3); live grader shape [UNVERIFIED until Monday 23:00 line].
- **Statistician**: finding — one code commit (197ed0d) + this bookkeeping ride the cover; ledger append-only.
- **Convexity Trader**: finding — study lift is +$15.95/tr E3 on quiet vs -$3.95 on busy (both 0/200 label-shuffle null); judged by $ mean-after-costs, not win-rate — grade from real rows.
- **Curl Mechanic**: finding — fire-count acceptance for both lanes starts Monday from rows; expect early v2 "not enough prior bars → not quiet" refusals.
- **Project Manager**: tags — rig ALL GREEN incl. AH [VERIFIED this session]; causality [VERIFIED by read + AH-ii/vi]; grader live [UNVERIFIED]; env flip = Marcos's explicit order, executed by main session not this convening; Deviation #1/#2 = Marcos-acknowledged.
- **Historian**: finding — for the record: first batch to convert TWO PRE sim-lanes at once; first lane (v2conv) with an explicitly CAUSAL trailing gate distinct from its calibrating study window.
- **Hidden Entry Architect**: finding — v2 is his sibling lane (anticipation on the fast chart); v2 shadow machine untouched, convert path added around it; both share `_nb`/`_vr_sv`.

## VERDICT
Room vote on "197ed0d (v2conv causal-quiet convert path + swept-in prevwap convert path, both PRE sim, env-OFF default) is ship-clean as audited: both convert flags default OFF with each `breakouts.append` single-sited and env-guarded; the v2 quiet gate is CAUSAL (reads only bars strictly before fire_k, buffer maintained before the step, rig AH-ii/vi green); PRE_LANES additions ride the convert flags so default-off leaves PRE_LANES untouched; neither lane in any exempt set, both PRE-capped + 09:25-flattened, v2conv capped 5/day; the prevwap work is one coherent batch with v2; Deviation #1 (causal vs descriptive) and Deviation #2 (10s vs 1-min → 89.9bps generous, week-1 observational) are Marcos-acknowledged, not blockers; rig ALL GREEN incl. AH + refreshed AF-i/Y-e/Z-d; no push, no deploy, no env flip from this convening": **31-0 APPROVE, 0 blocking**. Flat-book-verified-in-turn owed at the moment of deploy. Flipping the convert envs (Marcos's "have them all pre-market live" order) is executed by the main session — ALL SIM, DRY_RUN stays true.

SHIP_CHECK result (run after this bookkeeping commit): GREEN — see the chair's return message.

— Convening closed 2026-08-17 (clock cited above). Blast Radius Auditor, chair. No push, no deploy, no env flip from this convening.
