# SHIP CONVENING — 2026-08-14 (night, 21:57 ET) O-config SIM conversions: grinder lane + flat_top break-attack + E3 exits (commit 08e8639)
covers: 08e8639044a8 (the code commit — grinder conversion, flat_top break-attack, E3 exit mode, rig section AB; audited against `git show 08e8639` line-by-line THIS session). Prior convening archived to SESSION_20260814_e3wall.md.
Chair: Blast Radius Auditor. Separate-context convening: every claim below is from a read or an execution run this session — nothing carried on trust from the authoring session. This is the biggest behavior change of the day and it touches monitor_trade (the money path). DRY_RUN stays true all week per Marcos's sim-week ruling (commit 6a91056); Marcos's direct order authorizing the conversions: "i am saying sim money live not real life money."

## SHIP DESCRIPTION + AUTHORIZATION
Three defaults-ON knobs: GRINDER_CONVERT=1 (grinder_shadow_step fires ALSO convert to paper trades — lane "grinder", stop=would_stop, exit_mode=E3, cap 3/day, detector's 15-min cooldown; shadow rows unchanged), FLATTOP_BREAK_ATTACK=1 (9:30-10:30 ET flat_top breaks convert AT the break print, stop=base low, exit_mode=E3; skips dip/confirm + RETEST_ENTRY for this lane only), E3_EXITS=1 (exit_mode=E3 trades: bank 1/2 at +10% single tier, then 10%-off-run-high closes-through trail on completed 10s buckets; stop-first; _safety_close funnel; persists through restart).
Authorization: this DOES change what the bot does with (sim) money — authorized by Marcos's explicit order, evidence chain edge_stresstest_F (E3 PASS 5/5) -> edge_stresstest_G (TEST O PASS 5/5, mean +$156.64/d, median +$134.44/d). Auditor-cannot-authorize satisfied: the behavior was ordered by Marcos, this convening only verifies the build matches the order + evidence.

## FINDINGS (each verified by direct read/execution this session)
1. **E3 isolation — the critical blast-radius check — VERIFIED.** All eleven `_e3_mode` occurrences enumerated by grep (lines 9071/9075/9088/9527/9542/9562/9589/9620/9665 + init). Exactly five soft runner exits carry `not _e3_mode` guards: instant-exit (major reversal), prev-bar-low trail, topping-tail, rung-ratchet, health-fold. For every non-E3 trade `_e3_mode` is False, guards are inert, and the diff makes NO other change to those branches — line-by-line diff shows the only other monitor_trade edits are: signature param `exit_mode=None` (default preserves all callers), the `if _e3_mode: kev_tiers=[...] elif rocket/hidden ...` prepend (elif preserves the original chain verbatim), the E3-only trail block (guarded `if _e3_mode`), and the durable-state dict gaining an `exit_mode` key (data-only, None for other lanes). Other lanes: byte-identical behavior. Rig AB-e pins all five guards + surviving ladders.
2. **E3 mechanics vs the F-round sim — MATCHES with THREE disclosed deviations (see FIX/DISCLOSE list).** Verified against edge_stresstest_F_20260815.py (VAR E3: bank=0.50, tgt=0.10, trail="off10", be=None) and the .md method notes: run-high starts at ENTRY, updates from post-entry highs only (no lookahead, `_tape_birth` cut at line 9030 predates the block); trail = close < 0.90*run_hi (closes-through — a wick never fires, rig AB-d executes this case); stop-first tie (AB-d executes); single bank tier `[(entry*1.10, 0.50)]`; **NO breakeven after the bank — VERIFIED**: the BE ratchet is `tier_idx >= BE_FLOOR_AFTER_SCALE` and the default profile (kev25) has BE_FLOOR_AFTER_SCALE=2 while E3 has exactly one tier, so tier_idx maxes at 1 and the ratchet can never fire (matches F's be=None; avoids the E5 refuted free-roll). All exits funnel through _safety_close (rig Y-b: 16 paths, +1 = E3).
3. **Deviation A (the real finding): live E3 trails PRE-BANK; the F sim did not.** F's "off10" trail requires `scaled=True` — before the +10% bank fills, only the stop can exit. Live `_e3_eval` applies the 10%-off-run-high trail from bar one. Reachable band: close < 0.90*run_hi while still above the stop (e.g. run to +8%, close ~-3%, stop at -5%) exits live for a small loss where the sim held to stop/recovery. Direction: live is MORE conservative (cuts earlier, smaller losses, may forfeit recoveries). The F/G dollar evidence does not price this corner. NOT silently accepted: flagged to Marcos for a ruling — either accept the conservative variant or gate the trail on `partial_taken` (that change would need its own rig pass). Non-blocking for a DRY_RUN sim week; the sim-week tape itself will price it (query: E3 trail exits below entry with no prior tier_fill).
4. **Deviation B (pre-existing machinery, unmodeled in F): the early cuts still apply to E3 lanes.** Failed-breakout cut (fade to entry within ~75s unconfirmed) and early-VWAP-fade (<2 min) are lane-agnostic and unguarded — they can exit an E3 trade in ways the F sim never modeled. Pre-existing risk machinery, not touched by this diff, disclosed.
5. **Deviation C (minor): E3 stop check in `_e3_eval` is close-based; F stopped on the intrabar low.** Live keeps the tick-level intrabar stop machinery (INTRABAR_STOP live) unchanged, which covers the intrabar case; the 10s-close check is a backstop. Effectively equal-or-stricter than sim.
6. **Grinder conversion — VERIFIED.** Cap: `_gr_conv_day` day-keyed ledger, `>= GRINDER_DAILY_CAP` (default 3) logs `grinder_capped` and refuses; degenerate-stop guard `would_stop < px` on convert; shadow row ALWAYS logs first (evidence unchanged — the append sits after the `grinder_shadow_fire` write inside the same guard chain). **No exempt-set membership**: grepped every exempt tuple this session — BACKSIDE_EXEMPT={"dip_rip"}, MIN_STOP_EXEMPT default {zone_flip,hidden_entry,flat_top}, VRIDE_EXEMPT={hidden_entry}, _STALE_EXEMPT tuple, RETEST_LANES {flat_top,ignition,orb} — "grinder" appears in none; the full gate stack applies normally downstream. Post-10:30 by detector construction. Rig AB-b pins lane/exit_mode/stop/cap/gate-normality.
7. **Break-attack — VERIFIED.** Window test `"09:30" <= _hm_ft < "10:30"` sits INSIDE the fresh-break branch (`is_flat and price > w_high and not _pb`); out-of-window falls to the original arm/observe path verbatim (break_armed + pullback machinery untouched). VWAP gate (broke_no_vwap), daily-first veto, and room gate all sit downstream of `_pb_enter or _ft_attack` — break-attack passes through them unchanged. Stop override `round(w_low,4)` = TEST L spec. Observe-only strip bypass keyed `not b[4].get("break_attack")` — out-of-window flat_top still observe when FLATTOP_CONVERT=0. RETEST bypass keyed ONLY on `(extra or {}).get("break_attack")` — flat_top's other entries + ignition/orb still retest-wait. Note (priced, ordered): flat_top sits in MIN_STOP_EXEMPT by pre-existing default, so a paper-thin base-low stop won't be floored — pre-existing lane property, not a change in this diff.
8. **Doctrine inversion — DECLARED, not hidden: break-attack inverts the 8/6 retest doctrine.** The 8/6 RETEST_ENTRY doctrine ("wait for the pullback instead of buying the print" — dead-duck data, break bars at median 4% of peak volume) is inverted for this one lane, BY EVIDENCE: round G TEST L, break-attack beats retest on EVERY column — 61% win vs 37%, +$9,220 vs +$1,280, shallower worst day; the retest was filtering OUT the breaks that never pull back — plus Marcos's direct order. Priced and scoped: in-window flat_top only; every other lane keeps the retest doctrine.
9. **exit_mode persistence — VERIFIED end-to-end.** Worker call site passes `exit_mode=(extra or {}).get("exit_mode")`; the durable state dict written by _save_open_trade's payload carries `"exit_mode": exit_mode`; on restart `_recover_orphaned_trades` passes the row as `resume_state` and monitor_trade picks it up via `exit_mode = resume_state.get("exit_mode")`; `_e3_runhi` re-seeds from `max(entry, resume_state["highest"])`. Rig AB-d pins all three anchors.
10. **Rig executed MYSELF**: `python3 rig/test_shipset_20260804.py` -> **ALL GREEN, exit 0**, sections A..Z + AA + AB (AB-a..AB-e all green; AB-d EXECUTES `_e3_eval` on the synthetic scenario). Rig pin revisions honest: AA-b's zero-conversion pin explicitly marked SUPERSEDED by Marcos's order in the rig source itself; Y-b count 15->16 documented as the E3 path; Y-e/Z-d scan-range fixes correct (v2 block only, since grinder now legitimately converts).
11. **20260730 rig's one RED re-verified as PRE-EXISTING by my own execution**: ran `rig/test_shipset_20260730.py` at HEAD (RED: "reject logs the full ticket + refunds the slot", 1 failing) AND in a clean worktree at parent 6a91056 — same single RED. Not this ship's regression; did not re-run the stash check on trust.
12. **Boot visibility**: all three knobs + cap ride the banner AND the durable boot_config row (#26).

FIX-NOW LIST: none blocking. OWED TO MARCOS (a ruling, not a fix): Deviation A (pre-bank trail) — accept-as-conservative vs gate-on-partial_taken; sim-week tape will price it. Tracked non-blocking: Deviation B disclosure rides the week's E3 exit-reason rows; grinder/break-attack dashboard strip labels (Curator queue); flat-book-verified-in-turn at any deploy (none tonight).

## DAY-ONE WALKTHROUGH (Monday 8/17, default env, DRY_RUN=true)
- 9:30-10:30: flat_top breaks in the watch convert AT the print — `break_attack` decision row then `triggered_flat_top` with break_attack=True, stop = base low, exit_mode=E3. A break_attack conversion AFTER 10:30 = defect, pull the cord (out-of-window must show break_armed only).
- Post-10:30: `grinder_shadow_fire` rows as before; up to THREE `triggered_grinder` rows max (then `grinder_capped`). A triggered_grinder before 10:30 or a fourth in a day = defect.
- E3 trades: exactly ONE tier_fill (half at +10%) then either the E3 trail row ("E3 TRAIL (10% off run-high ...)"), an E3/intrabar stop, an early cut (Deviation B — count these), or EOD flatten. A second tier_fill on an E3 trade = defect. A trail exit BELOW entry with no prior tier_fill = Deviation A firing — count and price for Marcos's ruling.
- Every other lane (hidden, reclaim, zone_flip, ignition, orb, dip_rip, ma_pullback, vwap_reclaim...): exit behavior IDENTICAL to Friday — any changed exit reason on a non-E3 lane = blast-radius breach, pull the cord.
- Restart drill (if one happens): a resumed E3 trade must show E3 exits, not the ladder — `exit_mode` in its durable row is the check.

## DOCTRINE-INVERSION SWEEP
Retest doctrine (8/6): INVERTED for in-window flat_top ONLY — by round-G evidence + Marcos's order, declared in finding 8, scoped, killable (FLATTOP_BREAK_ATTACK=0). Chart-as-gate: intact — grinder/break-attack are tape/structure lanes; the gate stack (backside, min-stop, daily veto, room) applies per finding 6/7. Maps-describe: stops are computed structural anchors (15-min low, base low), no invented rungs. Edge-over-mechanisms: this ship IS the edge motion (O config to sim). 8/8 smaller+later: honored — sim week, DRY_RUN true, real money off. Fictional-fill law (8/13): honored — `_tape_birth` cut in the E3 block. Strength doctrine: break-attack BUYS strength at the print — the Ombudsman's direction.

## ROLL CALL (every ROSTER.txt office, 31)
- **Blast Radius Auditor** (chair): finding — diff read line-by-line; all `_e3_mode` sites enumerated; exempt tuples grepped; both rigs executed (incl. parent-commit worktree control); Deviations A/B/C surfaced instead of buried.
- **Trade Manager** (LEAD interest): finding — E3 is his nominated exit shipped to sim; Deviation A is HIS docket item for Marcos; BE-never-fires (finding 2) protects the F evidence from the E5 failure mode.
- **Systems Quant**: finding — `_e3_eval` computes what its docstring claims (pure, rig-executed); kev_tiers/elif chain preserves every other ladder verbatim; `_gr_conv_day` cap math correct incl. day rollover.
- **Wind Tunnel Engineer**: finding — fidelity audit produced Deviations A (pre-bank trail, sim never priced it), B (early cuts unmodeled), C (close-vs-low stop, covered by intrabar machinery); sim-to-live reconciliation = the week's job.
- **Execution Surgeon**: finding — bank fills via the existing tier machinery (tape-high/limit semantics unchanged); grinder entry px = stream price at convert (fire px fallback), vs sim's +1% chase slip — reconcile offered-vs-filled on the week's rows.
- **Momentum Operator**: finding — nothing here ships on noise: F 5/5 + G 5/5 behind every knob; the one unevidenced corner (Deviation A) is named, not waved.
- **Convexity Trader**: finding — E3 chosen on mean+median+worst-day; Deviation A clips left tail further but may also clip recoveries — the week's distribution answers it.
- **Strength Ombudsman**: finding — break-attack is the direct remedy to the retest's measured strength-refusal (TEST L: retest filtered out the breaks that never pull back); log a bias-ledger WIN.
- **Side Marshal**: finding — break-attack window rows carry the SIDE stamp via the normal gate path; backside gate applies to grinder normally (verified, no exemption).
- **Crown Steward**: finding — crowned names get no special E3 handling; grinder cap 3/day applies to crowns too — if a crown grinds past the cap, that's money-on-the-table evidence for his EOD scorecard, not a bypass request.
- **First Hour**: finding — break-attack lives entirely in his window; his daily attribution now includes the lane; 10:30 boundary rows are his to police (walkthrough item).
- **Opening Bell**: clean — nothing pre-9:30 changes; PRE machinery untouched by this diff.
- **Seam Scientist**: finding — beginning-entry doctrine advanced: the break print IS the beginning; OOS wall continues in parallel (shadow rows still always log — verified).
- **Forward Architect**: finding — hypothesis->kill-test->ship template executed again (KT2a -> H -> F -> G -> O -> sim); Deviation A registered as a new hypothesis with the week as its test.
- **Pit Crew Chief**: finding — three independent kill switches (GRINDER_CONVERT=0 / FLATTOP_BREAK_ATTACK=0 / E3_EXITS=0, the last reverting E3 lanes to the ladder); no deploy tonight so no flat-book check owed yet; restart semantics verified (finding 9).
- **Integrator**: finding — conversion rides the existing breakouts tuple + hidden_entry extra-dict pattern; single new call-site edge (worker passes exit_mode); no parallel exit logic — E3 replaces the ladder via the same kev_tiers variable, one seam.
- **Feed Engineer**: finding — E3 trail polls `_curl_feed(n=12)` every 10s per open E3 trade — bounded, same store the detectors ride; no new vendor surface.
- **Webull Broker Desk**: clean — DRY_RUN true; order semantics unchanged; _safety_close funnel intact (Y-b 16 paths); $5 place+cancel docket unchanged.
- **Quartermaster**: clean — no storage change; shadow rows + decision rows land in the same store; sim-week rows are the week's evidence corpus, back it up nightly.
- **Kev Librarian**: finding — Kev buys breaks of flat tops with size at the print (corpus-consistent); the 8/6 retest was OUR overlay, not Kev's — inversion moves TOWARD the bible; no contradiction.
- **Historian**: finding — for the record: 8/14 night = the first conversion of a nominated O-config to sim-live under Marcos's "sim money live" order; retest-doctrine inversion logged with its evidence lineage.
- **Statistician**: finding — every conversion/exit writes ledgered rows (triggered_grinder w/ day_n, break_attack, tier_fill, E3 exit reasons); week's grading queryable; unledgered number = rumor, none found.
- **Tape Veteran**: hypothesis — Monday-morning gap regimes may make 9:30-10:35 flat-top "bases" out of overnight ranges; watch the first session's break-attack quality vs the G sample (recorded, no action).
- **Reclaim Architect**: clean — reclaim/zone_flip paths byte-identical (guards inert); his lanes keep their exits.
- **Hidden Entry Architect**: clean — hidden_entry ladder branch preserved verbatim in the elif chain; his v2 rebuild unaffected; grinder occupying slots is a field observation for his N.
- **Handicapper**: clean — selection unchanged; both lanes iterate the existing candidates board.
- **Rocket Rider**: finding — rocket/hidden %-ladder confirmed intact (rig AB-e pin); grinder remains the measured vertical-regime answer, now with money-shaped evidence incoming.
- **Cartographer**: clean — no map rungs invented; base low / 15-min low / session high are computed lines.
- **Curl Mechanic**: finding — fire-count acceptance for the week: grinder conversions ≤3/day by construction; break-attack expected LOW single digits/day (G: in-window flat_top breaks are scarce) — tens/day = gates not executing, pull the cord.
- **Dashboard Curator**: finding — owed: lane badges for grinder/break-attack + an E3 exit-mode chip on the trade tale-of-the-tape (queue); boot banner + boot_config row shipped WITH the mechanism (verified).
- **Project Manager**: finding — tags: shipset ALL GREEN incl. AB [VERIFIED x1 plain + x1 SHIP_CHECK]; 20260730 single RED [PRE-EXISTING, verified at parent by worktree run]; Deviation A [OWED: Marcos ruling]; Monday walkthrough items registered.

## VERDICT
Room vote on "08e8639 is ship-clean as audited (sim conversions per Marcos's direct order, E3 isolated, gates normal, evidence-matched with three disclosed deviations, no deploy, no push)": **31-0 APPROVE, 0 blocking**. Deviation A goes to Marcos as a priced ruling request, not a silent acceptance; the sim week is itself the instrument that prices it. DRY_RUN stays true all week.

— Convening closed 2026-08-14 21:57 ET. Blast Radius Auditor, chair.
