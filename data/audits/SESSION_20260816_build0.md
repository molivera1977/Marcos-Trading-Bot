# SHIP CONVENING — 2026-08-16 (Sunday, 11:08 ET) BUILD #0: the eyes snapshot at entry/exit + exit_layer + tale render
covers: c5285f9d1eb5 (HEAD at audit; code = acf89f85c11b "build #0: eyes snapshot at entry/exit + exit_layer + tale render"; c5285f9 = data/killtests Sunday afternoon studies, analysis-only, zero runtime files — verified by `git show --stat`). Prior convening archived to SESSION_20260814_deadeyes.md.
Chair: Blast Radius Auditor. Separate-context convening: every claim below is from a `git show`/file read or an execution run THIS session. Clock: `date` run this turn = Sun Aug 16 11:08:34 EDT 2026. Worktree clean at audit start (only the archive rename is staged for the bookkeeping commit). No push, no deploy from this convening.

## SHIP DESCRIPTION + AUTHORIZATION
INSTRUMENTATION ONLY (Marcos 8/15: "where each ticker was at entry and exit ... every element of the EYES"). One code commit, acf89f8:
- **marcos_trading_bot.py (+289)**: `_eyes_snapshot(ticker, price, when, extra)` (:9080) assembles every eye into one dict; `_eyes_compact` (flat form for shadow rows); `_exit_layer` (pure string classifier over exit_reason); `_eyes_note_gate` (40-entry ring per ticker, fed from `_log_decision`, → `gates_hit`). Wire site 1 (entry, :12049): after the fill, `extra["entry_context"]` stamped + registry `_entry_ctx_by_trade[trade_id]` + carried into `_save_open_trade_sync` and the watchdog ctx. Wire site 2 (exit, :12202): record dict gains `entry_context`/`exit_context`/`exit_layer`; `post_to_dashboard` (:10207) choke point back-fills the three fields when the writer didn't (watchdog / resume / recovery records — those three carry `entry_context` from durable state). Shadow rows (v2_shadow_fire, grinder_shadow_fire, hidden_observe_only, observe-lane rows) gain an `eyes=` field.
- **screener_app.py (+105)**: display-only — `eyes_blocks_html` (Python) + `eyesHTML` (JS) render entry|exit side by side on the tale page, premarket trade history, and the closed-story card; `exit_layer` label on the "why it ended" line.
- **rig (+130)**: section AD (AD-a executes `_eyes_snapshot`, AD-b anchors the wire sites, AD-c executes the render incl. malformed blocks); `_side_state(` pin 4→5.
Authorization class: observe-only additions (auditor-safe, 8/13). No gate, sizing, exit, or order-path logic touched — no Marcos ruling required. One latency nuance disclosed in finding 3.

## FINDINGS (each verified by direct read/execution this session)
1. **Fail-soft verified line-by-line.** `_eyes_snapshot` pre-builds every top-level key (all None) BEFORE any try; every eye group (time, price, feed read, VWAP/slope, side, map, day gain, crown, lens, ext90, halt distance, spread, ambient, gates_hit) sits in its own `try/except: pass` (side falls to "unknown", crown to a None dict). Nothing between the try blocks can raise (dict/list literals only). `_eyes_compact` and `_exit_layer` are wrapped / pure `str()` ops. `_eyes_note_gate` is internally try-wrapped AND sits inside `_log_decision`'s outer try. Conclusion: no exception path from the snapshot into entry/exit code. Rig AD-a executes the real function with a stub feed and asserts every key present + TODO eyes None.
2. **Cache-backing of the reads — the honest picture.** `_effective_map` = 20s memo (`_effmap_cache`), backed by `_fetch_kev_levels` (90s TTL). `_recorder_tick_vwap` = 45s memo; on miss it is an HTTP GET to the dashboard `/api/bars` (6s timeout, up to 2 suffixes). `_lens_px` = registry peek, no REST. `_curl_feed` is the existing T2 choke point: with `CURL_SOURCE=webull` (code default) it is a locked dict copy of the in-process shadow bars (pure memory); with `CURL_SOURCE=alpaca` it is `_alp10_bars` = HTTP GET to the capture `/hot` endpoint (2.5s timeout, dashboard `/api/bars` fallback 5s). Production value of CURL_SOURCE is [UNVERIFIED] from this seat (Railway env not read here). Bounded either way: the snapshot reads the feed at most 3× (`n=720` direct, `n=360` inside `_side_state`, `n=720` inside `_marked_runway`), each capped at 720 bars, all loops O(n). No per-call full-history recompute; the EMA90 recompute is over ≤120 1-min buckets from the already-fetched window and only when the lane didn't donate `ema90`. `_curl_feed(n=720)` may trigger the ONE-SHOT-per-name `_archive10_backfill` and the pre-existing halt-suspect/leader-probe side effects — those are the same effects every zone computation already fires; a new call site of an existing choke point, not new behavior.
3. **The one money-path nuance: fill-to-monitor latency at wire site 1.** The entry snapshot runs synchronously AFTER the fill and BEFORE `_save_open_trade_sync` and `monitor_trade`. Cost = finding 2's reads. Same block ALREADY calls `_side_state(ticker, entry_price)` (:11985) and `_marked_runway` (:12089) at fill time, so the added exposure is ~2 extra feed reads of the identical class: microseconds under webull-source, sub-second typical under alpaca-source, worst case (all three GETs at timeout) ≈ 7.5–13s before the monitor starts. Not blocking (pre-existing class, bounded, DRY_RUN week); recommended follow-up: if CURL_SOURCE=alpaca in prod, memoize `_curl_feed` per (ticker, n) for ~2s or move the entry snapshot after `_save_open_trade_sync`. Exit-side snapshot runs after the position is already flat (record construction) — latency there delays only the record post.
4. **Entry/exit wires additive.** Diff read: no existing key removed or re-valued; `extra` promoted from None to `{}` only inside the new try (downstream uses are `(extra or {}).get(...)` — enumerated: no `if extra is None` / `**extra` / `dumps(extra)` consumer exists in the file), then gains one key `entry_context`. Record dict: three new keys only. `post_to_dashboard`: new fields set only when absent; registry entry popped after use (no unbounded growth on the normal path; an un-posted trade leaves one small dict — noted, negligible). Control flow (order placement, monitor, watchdog, recovery) untouched.
5. **Shadow rows: field-only.** All four sites (v2 :7372, grinder :7397, hidden :7708, observe rows :8283) add `eyes=` as a kwarg to an existing `_log_decision` call; no condition, ordering, or other field changed. Rig AD-b anchors all four.
6. **`_side_state` pin 4→5 legitimate.** `git show acf89f8` touches zero lines inside `_side_state` (:8824-8859 unchanged); the 5th call is `snap["side_stamp"] = _side_state(ticker, px)` — a stamp, consumed by nothing (Marshal's data-only law intact). Rig line 697-706 still executes the real function.
7. **screener_app.py display-only + robust.** `_eyes_get` walks dotted keys returning None on any non-dict; `_eyes_fmt` handles None/""/[]/bool/float/list; `_eyes_table_html` accepts non-dict ctx (renders the "no eyes block" row); all values `html.escape`d; tale block wrapped in try/except that renders the error text instead of 500ing; JS `eyesHTML` returns '' on any throw and on records without the fields. No new endpoint, no write path. Rig AD-c executes with missing and malformed blocks.
8. **Rig executed MYSELF**: `python3 rig/test_shipset_20260804.py` → **ALL GREEN, exit 0**, sections A..Z+AA+AB+AC+AD (AD-a/b/c green). Section Q warned "HEAD c5285f9 NOT yet audited" pre-artifact, as designed. SHIP_CHECK=1 run after the bookkeeping commit — result at the foot.
9. **Per-fire cost estimate** (asked explicitly): webull-source ≈ 1–5 ms (three ≤720-bar dict copies + O(n) passes + memo hits); alpaca-source ≈ 3 HTTP GETs typical 50–300 ms each when the capture box is hot, hard-bounded by timeouts. v2 shadow at "dozens/day" ⇒ worst-case tens of seconds of cumulative added wall time per day inside the candidate loop; no accumulation across fires (rings capped at 40 statuses/ticker, registry popped on record).

FIX-NOW LIST: none blocking. Tracked: (a) finding 3 memoization / ordering follow-up if prod runs alpaca-source; (b) `_entry_ctx_by_trade` leaks one dict per never-posted trade (restart clears); (c) `_gate_seen` grows one 40-entry ring per ticker seen per process life — cap is per-ticker only (hundreds of names/day × 40 tuples = trivial); (d) flat-book-verified-in-turn owed at deploy by whoever deploys.

## DAY-ONE WALKTHROUGH (Monday 8/17, default env, DRY_RUN=true)
- **Every conversion** (any lane, PRE or RTH): fill → `_eyes_snapshot(...,"entry")` → `extra["entry_context"]` → durable open-trade row carries `entry_context` (inspect via the open-trades store) → watchdog ctx carries it. Expected within the same second as the fill.
- **Every completed record**: normal monitor return → record dict has `entry_context` (from extra or registry), `exit_context` (snapshot at exit_price, when="exit"), `exit_layer` (e.g. "e3_trail", "stop", "eod", "safety"). Watchdog / resume / recovery records: `entry_context` from durable state; `exit_context`+`exit_layer` back-filled at `post_to_dashboard`. A completed record on the dashboard with `exit_context` missing = the choke point regressed — pull the cord.
- **Every shadow fire** (v2_shadow_fire, grinder_shadow_fire, hidden_observe_only, observe rows): decision row carries `eyes={px,vwap,vside,...,win}` compact block. Absence = the kwarg site regressed.
- **gates_hit**: on any snapshot, the last-5-min statuses for that name (≤12) — empty list on a name with zero decision rows in the prior 5 min is legitimate.
- **Dashboard**: tale page for any traded name shows "Where it was — the eyes at entry and exit" with two side-by-side tables + exit layer; premarket trade history rows show the same under the details; pre-8/16 records render the muted "no eyes block" line, never an error.
- **Passive canary**: zero fill-to-monitor gaps > 15 s in the log on conversion days (finding 3).

## DOCTRINE-INVERSION SWEEP
doctrine-inversion sweep: n/a — instrumentation only; no rule about who trades, what gates, or what is exempt changes. Every eye is a stamp consumed by nothing.

## ROLL CALL (every ROSTER.txt office, 31)
- **Blast Radius Auditor** (chair): finding — diff read line-by-line; observe-only class confirmed; the one money-path nuance (fill-to-monitor latency, finding 3) surfaced with a bound and a follow-up, not buried.
- **Systems Quant**: finding — `_eyes_snapshot` computes what its docstring claims (rig AD-a executes it); `_exit_layer` order-of-checks verified (safety before eod before trail before stop; "trail" precedes "stop" so "trail stop" classes as trail — intended per the docstring); VWAP slope is a feed-cum PROXY (labeled as such in code), halt distance uses tiered LULD bands.
- **Dashboard Curator**: finding — HER law honored: the mechanism ships WITH its display in three places (tale, history, closed-story card).
- **Pit Crew Chief**: finding — no kill switch needed (fail-soft stamps); no deploy from this convening; flat-book check owed at deploy time; finding 3 latency noted for his failure-domain registry.
- **Integrator**: finding — all three wire sites + four shadow sites + three recovery carries enumerated (rig AD-b); one choke point (`post_to_dashboard`) guarantees the record fields regardless of writer.
- **Side Marshal**: finding — `_side_state` untouched; the 5th call is a stamp into the snapshot (data-only law intact).
- **Crown Steward**: finding — crown status (`crowned/since/why`) now stamped on every entry/exit — his privilege-delivery audit gains the at-fill crown state.
- **Feed Engineer**: finding — up to 3 `_curl_feed` reads per snapshot; under alpaca-source those are HTTP to the capture box (bounded by 2.5s/5s timeouts); prod CURL_SOURCE [UNVERIFIED] here — his call whether to memoize (finding 3).
- **Webull Broker Desk**: clean — no order-path or account change.
- **Quartermaster**: finding — trade records grow by two nested dicts (~30 keys each) + one string; decisions rows gain `eyes` on shadow fires; archive expectations to be updated.
- **Kev Librarian**: clean — no Kev-corpus surface touched; `map_src` merely reads `_freshest_src`.
- **First Hour**: finding — `window` eye labels 09:30-10:30 explicitly; his attribution gets the at-fill block per trade.
- **Opening Bell**: finding — `session`/`window`="pre" stamps on PRE conversions; no lead-up logic touched.
- **Seam Scientist**: clean — seam lanes untouched; observe rows gain the compact eyes (more evidence, no logic).
- **Strength Ombudsman**: finding — `gates_hit` (last-5-min statuses) at fill lets refused-vs-taken strength be read from the record itself.
- **Forward Architect**: finding — EYES_TODO registered (relvol_same_tod, spy_regime, catalyst, ext_atr_units) stamped None until sources exist — his backlog.
- **Momentum Operator**: clean — no gate logic touched.
- **Trade Manager**: finding — `exit_layer` names the closing mechanism on every record (mandate #2 evidence path now exists); exit logic itself unchanged.
- **Tape Veteran**: hypothesis — a snapshot at the FILL is not the read at the SIGNAL; where extra donates lane values (vwap/day_gain/spread) they match the gate's numbers, the rest are fill-time — label accordingly when grading (recorded, no action).
- **Reclaim Architect**: clean — reclaim/zone_flip untouched.
- **Execution Surgeon**: finding — finding 3: fill-to-monitor latency adds ~2 feed reads of the pre-existing class; bounded; his canary = fill-to-monitor gap in the log.
- **Handicapper**: clean — selection untouched.
- **Rocket Rider**: clean — hidden_observe_only gains a compact eyes field only.
- **Cartographer**: finding — map eyes (break, break_dist, next_rung, zone, road_r post-wall, age, src) read through `_effective_map`/`_map_freshness`/`_marked_runway` — the same view the gates use, so map-vs-outcome grading uses gate-identical numbers.
- **Wind Tunnel Engineer**: clean — no backtest surface touched; the killtests tip (c5285f9) is analysis-only.
- **Statistician**: finding — new record fields `entry_context`/`exit_context`/`exit_layer` and decision field `eyes` registered; records with `exit_layer="unknown"` = missing/odd exit_reason, not a stamp failure.
- **Convexity Trader**: clean — no distribution-shaping change.
- **Curl Mechanic**: finding — fire-count acceptance: exactly one snapshot per conversion + one per completed record + one per shadow fire; `_gate_seen` ring 40/ticker.
- **Project Manager**: finding — tags: shipset ALL GREEN incl. AD [VERIFIED]; observe-only class [VERIFIED]; prod CURL_SOURCE [UNVERIFIED]; Monday walkthrough items registered above.
- **Historian**: finding — for the record: build #0 lands the day after the 8/15 eyes audit; the first ship whose promise is "answer where each ticker was at entry and exit."
- **Hidden Entry Architect**: clean — v2 shadow rows gain the compact eyes; v2 logic untouched.

## VERDICT
Room vote on "acf89f8 (HEAD c5285f9 incl. analysis-only killtests tip) is ship-clean as audited (instrumentation only, fail-soft, additive wires, display-only render, rig ALL GREEN incl. AD, one bounded latency nuance disclosed, no doctrine touched, no push/deploy from this convening)": **31-0 APPROVE, 0 blocking**. Flat-book-verified-in-turn owed at the moment of deploy.

SHIP_CHECK result (run after the bookkeeping commit): see the RESULTS_LEDGER entry / the chair's return message.

— Convening closed 2026-08-16 (clock cited above). Blast Radius Auditor, chair.
