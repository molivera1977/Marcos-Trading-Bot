# #54 BUILD SPEC — written BEFORE code (hostile-tape law). 8/13 evening.
## Build 1 — BLUE-SKY FIRST READS (reader)
First-read validate: read otherwise valid + max target <= live + FRESH 10s print in hand ->
POST with blue_sky=true (instead of discard). Targets kept as historical-support (advisory).
Bot-side TTL: a blue_sky map older than BLUESKY_TTL_SECS (default 600) counts as ABSENT for the
mapless check (fail-closed back to caged) — the 9:31-FGI-into-halt-down class dies here.
Blue-sky maps skip the read_exhausted/ceiling standdown (targets advisory by definition);
chart lanes stay protected by the LIVE break-side gate (entries >2% above break block, proven
firing today on HCTI/INHD). Manual maps may set blue_sky=true (the ceiling side-effect cure).
WRONG WHEN: a garbage read posts blue-sky and tape lanes repeatedly buy a dead name — guarded by
fresh-print requirement, TTL, and every lane gate still applying. KILL: BLUESKY_FIRSTREAD=0.
## Build 2 — KEV NAMES READ UNCONDITIONALLY (bot read-list)
Sheet names bypass the read-list activity/liquidity floor (floor unchanged for discovery names
and ALL entries). WRONG WHEN: budget burned on dead sheet names — bounded: sheet is 3-8 names,
reader dedupes one accepted read/name/day. KILL: KEV_READ_UNCONDITIONAL=0.
## Build 3 — AUTO-READ ON MAPLESS FIRES (bot)
mapless_reject -> POST ticker to /api/read_list + read_requested row; throttle 1/name/30min.
Reader picks it up next cycle. Display: mapless_reject joins both reject strips (🗺️ mapless).
WRONG WHEN: junk fires spam the reader — guarded by throttle + reader's own attempt caps.
KILL: AUTOREAD_ON_MAPLESS=0.
## RIDER — seam heartbeat: hourly seam_beat observe row (liveness check failed 8/11-8/13: zero
rows of any status = dead-vs-quiet indistinguishable).
## SPECIMENS (rig section T asserts): FGI 8:01 must PASS (fresh blue-sky), FGI 9:31 must DIE
(TTL), PLAG 8/11 must BLOCK (no fresh print -> no post), XHG 9:09 judged by its own gates.
