# Bar-consumer class sweep — 2026-07-15

Defect class: prior-day/stale bars leaking into "current" reads (B14 stop, B16 VWAP/gates).
Rule: every DECISION consumer routes through `_fresh_session()` (today-UTC + ≤15min staleness;
empty ⇒ NO decision). Archival/backtest consumers intentionally keep full history.

| Site (line ≈) | Consumer | Verdict |
|---|---|---|
| 1663/1717/1737 | EOD archive & winner-sweep fetches (960 ext) | **N/A by design** — archival wants full history |
| 1793 | B12 shadow divergence sampler | **ROUTED** — comparison line must use today's REST close |
| 2068 | Day-2 observer (vwap/hi observation) | **ROUTED** |
| 2518 | Entry gate (session stats inside check) | **ROUTED** |
| 3859/3862 | Watch loop `bars` + `full_bars` acquisition | **ROUTED AT SOURCE** — covers EMA9/20/90, base, ignition, room, triggers |
| 3875 | Pre+RTH VWAP fetch `_vwb` | **ROUTED at consumer** (`_fresh_session(_vwb)`) |
| 4213 | Reclaim-subtype session-VWAP instrumentation | **ROUTED at consumer** |
| 4502 | Velocity-ride check | **ROUTED** — was RAW (no session filter at all) |
| 4735 | Monitor stop/exit fetch | **ROUTED AT SOURCE** + `_stop_close_qualifies` post-entry guard |
| 4851 | Health-fold session VWAP `_svb` | **ROUTED at consumer** |
| 4979 | SPY connectivity probe | **N/A** — liveness only |
| 5955 | Volume guard (tape/min sizing clamp) | **ROUTED** — stale tape must not size today |
| 6021 | Entry gates (topping-tail / liquidity) | **ROUTED** |
| `_vw_roll` (monitor) | rolling-45 VWAP | **SAFE** — input `bars` already fresh-routed at 4735 |

Interaction checks (fixes vs fixes, the B15 lesson):
- Staleness (900s) + B11: a 15-min bar-less halt eventually counts as sight failure → B11 can exit
  at last stream price. Halts aren't really exitable — existing DRY_RUN fill-realism caveat (B13 family), accepted.
- Premarket (RTH-only fetches → prior day → []): detectors idle until today's first RTH bars publish
  (~9:31–9:33). Correct: the bot must not reason from yesterday's bars premarket. Cosmetic: watch line
  shows blanks pre-9:32.
- UTC day key: session (4am–8pm ET) lives inside one UTC date; 8pm ET = UTC midnight exactly — nothing
  bar-driven runs after 8pm ET.
- Archive paths untouched — EOD dumps/backfills still capture full multi-day/extended history.

Rig: `python3 rig/test_defects.py` — 21/21 green at commit time.
